# cli/tests/test_bugfixes.py

12 class(es): TestUpdateCmdRefFormat, TestFetchArtifactDirOwner, TestAsyncMainIOStream, TestPostInstallConfirmation, TestFormatFrontmatterFixes, TestParseFrontmatterFixes, TestServeCmdYaml, TestReplayHtmlEscaping, TestLockfilePathSafety, TestDiscoveryCleanup, TestTypeDirSkillsMapping, TestClaudeTargetUninstallCount. 38 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestUpdateCmdRefFormat | class |  |
| TestFetchArtifactDirOwner | class |  |
| TestAsyncMainIOStream | class |  |
| TestPostInstallConfirmation | class |  |
| TestFormatFrontmatterFixes | class |  |
| TestParseFrontmatterFixes | class |  |
| TestServeCmdYaml | class |  |
| TestReplayHtmlEscaping | class |  |
| TestLockfilePathSafety | class |  |
| TestDiscoveryCleanup | class |  |
| TestTypeDirSkillsMapping | class |  |
| TestClaudeTargetUninstallCount | class |  |

## Chunks

### TestUpdateCmdRefFormat (class, L23-L100)

> *Summary: This test suite verifies the correct reference formatting used by the `update` command when comparing local lockfile entries against remote registry artifacts. It specifically tests scenarios where the update command correctly detects version mismatches or confirms that all installed components are up to date based on mocked registry responses.*


### test_registry_key_matches_lockfile_ref (method, L26-L36, parent: TestUpdateCmdRefFormat)

> *Summary: This test verifies that the artifact reference generated by `Artifact` matches the expected path constructed using a pluralization helper. It confirms consistency between how an artifact's reference is stored and how it is rebuilt from its type, owner, and name components.*


### test_update_detects_version_mismatch (method, L38-L70, parent: TestUpdateCmdRefFormat)

> *Summary: This test verifies that the `update` command correctly identifies a version mismatch when the local lockfile specifies an older version than what is available in the mocked registry. It simulates installing an artifact at version 1.0.0 and mocks the registry to return version 2.0.0, asserting that the output reflects the detected update.*


### test_update_all_up_to_date_when_versions_match (method, L72-L100, parent: TestUpdateCmdRefFormat)

> *Summary: This test verifies that the update command reports being up-to-date when the local lockfile versions match the remote registry versions. It simulates a scenario where one tool's version is recorded locally and fetched from a mock registry, asserting the output contains "up to date".*


### TestFetchArtifactDirOwner (class, L108-L143)

> *Summary: These tests verify artifact fetching logic, ensuring that artifacts from the "ag2ai" owner utilize a flat path structure while third-party owners use an owner-namespaced path when calling `fetch_artifact_dir`. The function under test receives artifact details and returns by interacting with mocked client methods to assert the correct directory paths are used.*


### test_default_owner_uses_flat_path (method, L111-L126, parent: TestFetchArtifactDirOwner)

> *Summary: This test verifies that when fetching artifacts owned by "ag2ai," the client uses a specific flat path structure instead of recursive listing. It mocks artifact retrieval to assert that `_list_contents_recursive` is called with the expected directory path, `"tools/web-search"`.*


### test_third_party_owner_uses_namespaced_path (method, L128-L143, parent: TestFetchArtifactDirOwner)

> *Summary: This test verifies that when fetching artifacts from a third-party owner, the client correctly uses a namespaced path structure (`type_dir/owner/name`). It mocks artifact listing and byte retrieval to assert that `_list_contents_recursive` is called with the expected namespaced directory string.*


### TestAsyncMainIOStream (class, L151-L169)

> *Summary: Verifies that an asynchronous `main` function executed within a thread pool correctly returns its expected output string when run via the CLI execution framework. It achieves this by writing a simple async script to a temporary file and invoking the discovery and execution pipeline against it.*


### test_async_main_returns_result (method, L154-L169, parent: TestAsyncMainIOStream)

> *Summary: This test verifies that the asynchronous main function executes correctly when run via the CLI framework. It writes a simple async script to a temporary file, discovers it, executes it with an input argument, and asserts the returned output matches expectations.*


### TestPostInstallConfirmation (class, L177-L224)

> *Summary: These tests verify the behavior of post-installation commands based on user input during an installation process. It asserts that subprocesses are skipped if confirmation is denied or if standard input is closed, but they execute when explicit confirmation is given.*


### test_post_install_skipped_when_user_declines (method, L180-L192, parent: TestPostInstallConfirmation)

> *Summary: This test verifies that post-installation commands are skipped when the user declines a prompt. It mocks the confirmation function to return `False` and asserts that no subprocess commands are executed during the installation process.*


### test_post_install_runs_when_user_confirms (method, L194-L210, parent: TestPostInstallConfirmation)

> *Summary: This test verifies that post-installation commands execute when the user confirms during an installation process. It mocks confirmation to return `True` and asserts that the underlying subprocess execution function is called exactly once with the specified commands.*


### test_post_install_skipped_on_eof (method, L212-L224, parent: TestPostInstallConfirmation)

> *Summary: This test verifies that post-installation commands are skipped when standard input is closed (EOF). It mocks the confirmation prompt to raise `EOFError` and asserts that no subprocesses are executed during the installation process.*


### TestFormatFrontmatterFixes (class, L232-L293)

> *Summary: This test suite verifies that the `format_frontmatter` function correctly handles various data types when converting Python dictionaries to YAML frontmatter strings. It specifically asserts correct formatting for lists, quoted/unquoted strings, and boolean/numeric values based on their input type.*


### test_list_values_formatted_as_yaml_array (method, L235-L239, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that a list of glob patterns is correctly formatted as a YAML array within frontmatter. It takes a dictionary containing globs and asserts the resulting string includes the expected YAML array representation.*


### test_empty_list_formatted (method, L241-L245, parent: TestFormatFrontmatterFixes)

> *Summary: When provided with a dictionary containing an empty list under the "items" key, this test asserts that the resulting formatted string explicitly includes `"items: []"`. This verifies correct handling of zero-length lists during frontmatter formatting.*


### test_yaml_true_is_quoted (method, L247-L251, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that when processing YAML frontmatter, the boolean value `true` is correctly quoted as a string within the output. It takes a dictionary containing `"value": "true"` and asserts the resulting formatted string includes `'value: "true"'`.*


### test_yaml_false_is_quoted (method, L253-L257, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that when the input YAML value for a key is the string `"false"`, the resulting frontmatter correctly quotes it as `value: "false"`. It calls `format_frontmatter` with a dictionary containing this specific string value and asserts the expected quoted output.*


### test_yaml_null_is_quoted (method, L259-L263, parent: TestFormatFrontmatterFixes)

> *Summary: Verifies that when processing YAML frontmatter, a string input of `"null"` is correctly quoted as `value: "null"` in the resulting output. This test ensures proper serialization behavior for null values within metadata structures.*


### test_numeric_string_is_quoted (method, L265-L269, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that when a numeric value is provided as a string within front matter, it is correctly enclosed in quotes in the resulting output. It calls `format_frontmatter` with a dictionary containing `"port": "8080"` and asserts the presence of `'port: "8080"'`.*


### test_actual_bool_not_quoted (method, L271-L275, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that when a boolean value is provided as input to the frontmatter formatting function, it is correctly rendered as an unquoted `true` string in the output. It asserts the presence of `"enabled: true"` within the resulting formatted string.*


### test_actual_int_not_quoted (method, L277-L281, parent: TestFormatFrontmatterFixes)

> *Summary: Verifies that an integer value provided in the input dictionary is correctly formatted without quotes when processed by `format_frontmatter`. The function asserts that the resulting string contains `"count: 42"` for the given input.*


### test_empty_string_is_quoted (method, L283-L287, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that when an empty string is provided for a field, the resulting front matter output correctly quotes the value. It calls `format_frontmatter` with a dictionary containing an empty string and asserts the presence of `"name: \"\""` in the returned string.*


### test_plain_string_not_quoted (method, L289-L293, parent: TestFormatFrontmatterFixes)

> *Summary: This test verifies that a simple dictionary input, like `{"name": "hello"}`, is correctly formatted into frontmatter where the string value remains unquoted. It asserts that the resulting string contains `"name: hello"`.*


### TestParseFrontmatterFixes (class, L301-L333)

> *Summary: This test suite verifies that the frontmatter parsing function correctly handles various list syntaxes, including bracketed lists with and without quotes. It ensures proper extraction of metadata (like tags or globs) from YAML blocks while preserving the main body content.*


### test_parses_bracket_list (method, L304-L310, parent: TestParseFrontmatterFixes)

> *Summary: This test verifies that the `parse_frontmatter` function correctly extracts a list of tags from YAML frontmatter enclosed in brackets. It takes a string containing frontmatter and body text, asserting that the parsed dictionary contains the expected list for the "tags" key and the remaining content is returned as the body.*


### test_parses_quoted_bracket_list (method, L312-L317, parent: TestParseFrontmatterFixes)

> *Summary: This test verifies that the `parse_frontmatter` function correctly extracts a list of strings enclosed in quotes within YAML frontmatter. It takes a string containing quoted bracket notation and asserts the resulting dictionary contains the expected list.*


### test_parses_empty_bracket_list (method, L319-L324, parent: TestParseFrontmatterFixes)

> *Summary: This test verifies that the `parse_frontmatter` function correctly handles an empty list defined within YAML frontmatter. It asserts that when the input contains `"items: []"`, the resulting frontmatter dictionary has an empty list for the "items" key.*


### test_still_parses_regular_values (method, L326-L333, parent: TestParseFrontmatterFixes)

> *Summary: This test verifies that the `parse_frontmatter` function correctly extracts key-value pairs from a string containing YAML frontmatter. It takes a combined text input and asserts that the resulting dictionary accurately reflects the parsed metadata, including boolean values.*


### TestServeCmdYaml (class, L341-L379)

> *Summary: This test suite verifies that the `serve_cmd` command correctly handles configuration file types. It asserts that files ending in `.yaml` trigger YAML-specific loading logic while ensuring that standard `.py` files continue to use the existing discovery mechanism, even when mocked to fail agent detection.*


### test_serve_yaml_routes_to_yaml_builder (method, L344-L355, parent: TestServeCmdYaml)

> *Summary: This test verifies that the `serve_cmd` command correctly implements logic to handle YAML configuration files. It achieves this by inspecting the source code of `serve_cmd` to confirm the presence of `.yaml`/`.yml` extension checks and relevant YAML loading functions.*


### test_serve_py_file_uses_discover (method, L357-L379, parent: TestServeCmdYaml)

> *Summary: This test verifies that when serving a Python file, the system correctly invokes the `discover` function instead of using YAML building logic. It mocks discovery to simulate an agent absence and asserts that `discover` was called exactly once during command execution with a provided Python file path.*


### TestReplayHtmlEscaping (class, L387-L477)

> *Summary: Tests verify that the HTML export feature correctly escapes user-provided content to prevent Cross-Site Scripting (XSS) vulnerabilities. It achieves this by running CLI commands against saved sessions containing malicious scripts in both event content and speaker names, asserting that the output contains escaped entities instead of executable code.*


### test_html_export_escapes_script_tags (method, L390-L435, parent: TestReplayHtmlEscaping)

> *Summary: This test verifies that the HTML export feature correctly escapes script tags to prevent XSS vulnerabilities. It takes a session containing malicious script content as input and asserts that the resulting HTML output contains escaped versions of the tag instead of raw, executable code.*


### test_html_export_escapes_speaker_name (method, L437-L477, parent: TestReplayHtmlEscaping)

> *Summary: This test verifies that the HTML export feature correctly escapes potentially malicious content within a speaker's name during session serialization. It inputs a session containing an XSS payload in the speaker field and asserts that the resulting HTML output does not contain the dangerous script tag.*


### TestLockfilePathSafety (class, L485-L526)

> *Summary: This test suite verifies that the `Lockfile` correctly handles file paths during installation recording. It ensures that files outside the designated project directory are ignored, and only relative paths within the project scope are stored in the installed information.*


### test_out_of_project_files_skipped (method, L488-L508, parent: TestLockfilePathSafety)

> *Summary: This test verifies that a lockfile correctly ignores files provided with absolute paths when recording an installation. It asserts that only relative, project-internal file paths are stored after calling `record_install`.*


### test_all_inside_project_files_kept (method, L510-L526, parent: TestLockfilePathSafety)

> *Summary: This test verifies that a `Lockfile` correctly records all specified files during an installation process. It inputs a temporary directory and a list of file paths, asserting that the retrieved installation information contains exactly those three recorded files.*


### TestDiscoveryCleanup (class, L534-L564)

> *Summary: This test suite verifies the behavior of an agent file importer regarding Python's `sys.modules`. It asserts that a failed import correctly cleans up the corresponding module from `sys.modules`, while a successful import leaves it present for subsequent use.*


### test_failed_exec_module_cleans_sys_modules (method, L537-L549, parent: TestDiscoveryCleanup)

> *Summary: This test verifies that when an agent file raises a runtime error during import, the corresponding module is correctly removed from `sys.modules`. It achieves this by attempting to import a deliberately faulty Python file and asserting its absence from system modules upon failure.*


### test_successful_import_stays_in_sys_modules (method, L551-L564, parent: TestDiscoveryCleanup)

> *Summary: This test verifies that successfully importing a local Python file adds the resulting module to `sys.modules` and correctly executes its contents. It imports an agent file from a temporary path and asserts both its presence in system modules and the correct value of an attribute defined within it.*


### TestTypeDirSkillsMapping (class, L572-L599)

> *Summary: This test suite verifies bug fixes related to how the system handles singular and plural forms of "skill." It asserts that helper functions correctly map both "skill" to "skills" and validates artifact reference generation using the "skills" type.*


### test_type_dir_skills_plural (method, L575-L578, parent: TestTypeDirSkillsMapping)

> *Summary: Verifies that the internal `_type_dir` method, when called with "skills", correctly returns the plural form "skills". This test ensures consistent directory naming conventions for skill-related artifacts.*


### test_type_dir_skill_singular (method, L580-L583, parent: TestTypeDirSkillsMapping)

> *Summary: Verifies that the internal `_type_dir` method, when given the input string `"skill"`, correctly returns the pluralized form `"skills"` using an instance of `ArtifactClient`. This test ensures consistent directory naming conventions for skill-related artifacts.*


### test_pluralize_type_skills_plural (method, L585-L588, parent: TestTypeDirSkillsMapping)

> *Summary: This test verifies that the internal pluralization function correctly handles a specific input, asserting that passing `"skills"` results in the unchanged string `"skills"`.*


### test_pluralize_type_skill_singular (method, L590-L593, parent: TestTypeDirSkillsMapping)

> *Summary: This test verifies that the internal pluralization function correctly converts the singular string "skill" to its plural form, "skills". It asserts this expected output using a direct call to `_pluralize_type`.*


### test_artifact_ref_uses_skills_correctly (method, L595-L599, parent: TestTypeDirSkillsMapping)

> *Summary: Verifies that an `Artifact` initialized with the type "skills" correctly constructs its reference path using the provided name and owner. The test asserts the resulting `.ref` attribute matches the expected format: `"skills/<owner>/<name>"`.*


### TestClaudeTargetUninstallCount (class, L607-L647)

> *Summary: This test suite verifies that the `ClaudeTarget`'s uninstall method correctly counts and reports only files, excluding directories, when cleaning up installed components. It asserts that uninstallation returns a list containing only paths to actual files from both skills and commands directories.*


### test_uninstall_counts_only_files (method, L610-L627, parent: TestClaudeTargetUninstallCount)

> *Summary: This test verifies that the uninstall process, when run against a specific target, only reports individual files as being removed, not the containing directories. It asserts that the returned list contains exactly two file paths corresponding to the setup content and excludes any directory entries.*


### test_uninstall_skill_and_command_counts (method, L629-L647, parent: TestClaudeTargetUninstallCount)

> *Summary: This test verifies that the `ClaudeTarget` correctly uninstalls both a single skill and a single command when provided with temporary directories containing these files. It asserts that exactly two items are removed during the uninstall process.*

