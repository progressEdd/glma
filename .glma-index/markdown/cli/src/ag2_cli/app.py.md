# cli/src/ag2_cli/app.py

1 function(s): main.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| main | function |  |

## Chunks

### main (function, L20-L34)

> *Summary: This function serves as the entry point for the CLI, displaying a banner and version information if requested via the `--version` flag. If no subcommand is invoked, it prints a usage prompt directing the user to view available commands.*

