# cli/src/ag2_cli/commands/serve.py

6 function(s): _require_fastapi, _build_rest_app, _build_mcp_server, _build_a2a_app, _start_ngrok, serve_cmd.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| _require_fastapi | function |  |
| _build_rest_app | function |  |
| _build_mcp_server | function |  |
| _build_a2a_app | function |  |
| _start_ngrok | function |  |
| serve_cmd | function |  |

## Chunks

### _require_fastapi (function, L12-L22)

> *Summary: Checks for the presence of `fastapi` and `uvicorn`; if either is missing, it prints an error message detailing installation instructions and exits the application. Otherwise, it returns the imported FastAPI and uvicorn modules.*


### _build_rest_app (function, L30-L84)

> *Summary: Constructs a FastAPI application instance configured to serve an agent's functionality. It takes a discovered agent object as input and returns the fully initialized `FastAPI` application, which exposes endpoints for health checks, listing agents, and performing chat interactions via asynchronous execution of the agent logic.*


### _build_mcp_server (function, L92-L129)

> *Summary: Constructs a FastMCP server instance that exposes the AG2 agent's capabilities as callable tools to external MCP clients. It takes discovered agent information and host/port settings, returning a configured `FastMCP` object ready for network serving.*


### _build_a2a_app (function, L137-L183)

> *Summary: Constructs an ASGI application exposing discovered AI agents via the A2A protocol, depending on whether a single agent or multiple agents are provided in the input discovery object. It returns either a configured `A2aAgentServer` instance for one agent or a `Starlette` application with mounted routes for multiple agents.*


### _start_ngrok (function, L191-L235)

> *Summary: This function initializes and starts an ngrok tunnel for a given port, first attempting to retrieve the necessary authentication token from environment variables or common configuration files. It returns the public URL generated by the active tunnel if successful, otherwise it exits with an error message.*


### serve_cmd (function, L243-L324)

> *Summary: This function launches an agent system by serving discovered agents as APIs (REST), MCP servers, or A2A endpoints based on command-line arguments. It takes the agent definition file path and configuration options like port, protocol, and ngrok enablement to start the appropriate server instance.*

