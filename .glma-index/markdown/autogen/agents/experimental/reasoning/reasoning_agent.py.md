# autogen/agents/experimental/reasoning/reasoning_agent.py

2 function(s): extract_sft_dataset, extract_rlhf_preference_dataset. 2 class(es): ThinkNode, ReasoningAgent. 24 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| ThinkNode | class |  |
| extract_sft_dataset | function |  |
| extract_rlhf_preference_dataset | function |  |
| ReasoningAgent | class |  |

## Chunks

### ThinkNode (class, L85-L246)

> *Summary: Represents a node in a tree structure used to track reasoning steps, storing content, statistical values (like `value` and `visits`), and hierarchical relationships with parents and children. It supports traversal methods to reconstruct the full reasoning path and includes functionality for serializing/deserializing its state via dictionaries or visualizing the entire thought process using Graphviz.*


### __init__ (method, L86-L123, parent: ThinkNode)

> *Summary: Initializes a tree node representing a reasoning step, accepting descriptive content and an optional parent node. It automatically calculates the node's depth, sets up parent-child relationships, and initializes various tracking attributes like value, visits, and output for subsequent processing.*


### _trajectory_arr (method, L126-L137, parent: ThinkNode)

> *Summary: Retrieves the complete path from the root node to the current node by recursively collecting content and output information from all ancestors. It returns this history as a list of strings, starting with a formatted question marker for the initial node.*


### trajectory (method, L140-L151, parent: ThinkNode)

> *Summary: Retrieves and formats the agent's reasoning path stored internally. It constructs a string starting with the initial question, followed by numbered steps detailing each subsequent action in the process.*


### backpropagate (method, L153-L163, parent: ThinkNode)

> *Summary: Updates the score of a node and all its ancestors by applying a moving average based on an incoming `reward`. It iteratively traverses up the parent chain, incrementing visit counts and recalculating the value for each encountered node.*


### __str__ (method, L165-L166, parent: ThinkNode)

> *Summary: Provides a string representation of the agent's state, including its content, search depth, associated value, and number of visits. This output is useful for debugging or logging the agent's current status.*


### __repr__ (method, L168-L169, parent: ThinkNode)

> *Summary: This method returns the string representation generated by `__str__()`, providing a developer-friendly, unambiguous output when the object is printed or inspected.*


### to_dict (method, L171-L186, parent: ThinkNode)

> *Summary: Converts the agent's internal state into a serializable dictionary format. It includes all node attributes and recursively transforms its children into dictionaries as well.*


### from_dict (method, L189-L211, parent: ThinkNode)

> *Summary: Reconstructs a `ThinkNode` object from a dictionary representation by initializing core attributes like content and value. It recursively processes the "children" list within the input dictionary to build the entire node hierarchy.*


### visualize_tree (method, L213-L246, parent: ThinkNode)

> *Summary: Generates a visual representation of the agent's thought process tree using Graphviz, provided the library is installed. It recursively traverses the `ThinkNode` structure, rendering each node with its content summary, visit count, and value as a PNG file named "tree\_of\_thoughts".*


### extract_sft_dataset (function, L249-L282)

> *Summary: This function extracts high-quality training examples from a thought tree structure by identifying all leaf nodes that share the maximum associated score. It returns a list of dictionaries, where each entry pairs the root instruction with the corresponding response trajectory from the top-scoring leaves.*


### extract_rlhf_preference_dataset (function, L285-L338)

> *Summary: Generates RLHF preference pairs by traversing a tree structure and comparing sibling nodes based on their accumulated values or visits against a specified contrastive threshold. It takes a root `ThinkNode` and returns a list of dictionaries, each defining a preferred and dispreferred response for training data.*


### ReasoningAgent (class, L342-L1177)

> *Summary: This class implements a Tree-of-Thought agent capable of complex problem-solving using various search strategies like Beam Search, MCTS, and LATS. It takes configuration for LLMs, reasoning parameters (depth, beam size), and code execution to generate reasoned responses from an input prompt, returning the final answer or a list of potential answers.*


### __init__ (method, L343-L510, parent: ReasoningAgent)

> *Summary: Initializes a tree-of-thought reasoning agent, configuring its behavior based on provided LLM settings and detailed `reason_config`. It sets up internal components like the thinker, grader, and optionally an executor/user proxy for iterative reasoning processes.*


### _add_scope (method, L512-L523, parent: ReasoningAgent)

> *Summary: Prepends a task scope string to an existing system prompt if the agent has defined scope information. It returns the original prompt unchanged if no scope is set.*


### generate_forest_response (method, L525-L579, parent: ReasoningAgent)

> *Summary: This method generates a response by executing multiple reasoning paths (based on `_forest_size` and `_method`) using either beam search/DFS or MCTS/LATS. It returns the result directly if only one path is run, otherwise it sends all generated answers to itself for final synthesis and returns that synthesized content.*


### rate_node (method, L581-L675, parent: ReasoningAgent)

> *Summary: Calculates a normalized quality score for a reasoning path or final answer by prompting an external grader agent. It constructs a detailed system message based on whether the rating is for an outcome or process, sends this prompt to the grader, and parses the returned numerical rating into a float between 0 and 1.*


### rate_batch_nodes (method, L677-L772, parent: ReasoningAgent)

> *Summary: Evaluates a batch of related thinking nodes by prompting an external grader agent with the current trajectory and options. It takes a list of `ThinkNode` objects and an optional ground truth string as input, returning a list of normalized float scores corresponding to each node's quality assessment.*


### execute_node (method, L774-L835, parent: ReasoningAgent)

> *Summary: Executes a thinking node by either sending its content to a user proxy for Python execution or prompting an LLM agent. It returns the resulting string response from the executed action, or `None` if the node is marked as terminal.*


### _process_prompt (method, L837-L900, parent: ReasoningAgent)

> *Summary: This method processes a list of messages to extract a refined prompt and any associated ground truth for evaluation. It either uses the single message as the prompt or delegates to another agent to summarize the conversation into a structured question and summary if multiple messages are present.*


### _beam_reply (method, L902-L997, parent: ReasoningAgent)

> *Summary: This method performs tree-of-thought reasoning by iteratively expanding nodes, evaluating paths using a grader agent, and maintaining the top $N$ promising branches via beam search. It takes a prompt and optional ground truth to generate and return the best resulting response string based on the chosen answer approach ("best" or "pool").*


### _mcts_reply (method, L999-L1071, parent: ReasoningAgent)

> *Summary: This method performs Monte Carlo Tree Search (MCTS) reasoning to generate a response based on an input prompt. It iteratively explores potential solution paths, simulates outcomes, and uses reinforcement learning principles to select the final answer from the most highly-rated terminal node found across multiple simulations. The output is the content of this best-found answer node.*


### _expand (method, L1073-L1113, parent: ReasoningAgent)

> *Summary: Requests the thinker agent to suggest next steps based on a given reasoning node's trajectory. It parses the thinker's response to extract potential reflection notes and create new `ThinkNode` instances for each suggested option, returning these new nodes.*


### _is_terminal (method, L1115-L1124, parent: ReasoningAgent)

> *Summary: Determines if a given `ThinkNode` represents a final state by checking if its depth exceeds a predefined maximum or if its content explicitly contains the string "TERMINATE". Returns a boolean indicating whether the process should stop at that node.*


### method (method, L1127-L1133, parent: ReasoningAgent)

> *Summary: Retrieves and returns a string representing the currently active reasoning methodology stored within the instance.*


### visualize_tree (method, L1135-L1144, parent: ReasoningAgent)

> *Summary: This method renders the agent's thought process as a graph using Graphviz, provided a root node exists. It raises an error if no reasoning tree has been generated prior to calling it.*


### extract_sft_dataset (method, L1146-L1158, parent: ReasoningAgent)

> *Summary: Retrieves the optimal or equally good execution paths from a generated reasoning tree for Supervised Fine-Tuning (SFT). It returns a list of dictionaries, where each dictionary pairs an instruction with its corresponding response.*


### extract_rlhf_preference_dataset (method, L1160-L1177, parent: ReasoningAgent)

> *Summary: Generates a list of preference pairs suitable for RLHF training by comparing sibling nodes within a generated tree structure. It takes a `contrastive_threshold` as input and returns dictionaries specifying preferred responses, raising an error if no tree exists.*

