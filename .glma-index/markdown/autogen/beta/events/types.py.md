# autogen/beta/events/types.py

9 class(es): Usage, ModelEvent, ModelReasoning, ModelMessage, BinaryResult, ModelResponse, ModelMessageChunk, HumanInputRequest, HumanMessage. 8 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| Usage | class |  |
| ModelEvent | class |  |
| ModelReasoning | class |  |
| ModelMessage | class |  |
| BinaryResult | class |  |
| ModelResponse | class |  |
| ModelMessageChunk | class |  |
| HumanInputRequest | class |  |
| HumanMessage | class |  |

## Chunks

### Usage (class, L15-L33)

> *Summary: Represents normalized token usage across various beta LLM providers by storing several floating-point metrics like prompt, completion, and total tokens. It evaluates to `True` if any of the defined token count attributes have a non-null value.*


### __bool__ (method, L25-L33, parent: Usage)

> *Summary: Determines the truthiness of an event object by checking if any of its token-related attributes (prompt, completion, total, cache reads/creations, or thinking) are non-empty. Returns `True` if at least one token count is greater than zero, otherwise returns `False`.*


### ModelEvent (class, L36-L37)

> *Summary: Serves as the foundational class for any event originating from a language model interaction. It inherits from `BaseEvent` to provide common event structure.*


### ModelReasoning (class, L40-L48)

> *Summary: Represents transient, intermediate thought processes generated by a model during execution. It holds a string containing this reasoning content and is marked as non-final output.*


### ModelMessage (class, L51-L62)

> *Summary: Represents a single message generated by the model, containing string content and optional metadata. This transient object is typically embedded within a `ModelResponse` rather than being stored persistently.*


### BinaryResult (class, L66-L77)

> *Summary: Represents a binary output from a model, holding raw byte data and associated metadata. It provides an asynchronous method to retrieve the underlying byte content and exposes a derived name based on the metadata's "filename" key.*


### name (method, L73-L74, parent: BinaryResult)

> *Summary: Retrieves the file's name from its metadata, defaulting to "generated\_file" if no filename is present. This method returns a string representing the identifier for the object.*


### content (method, L76-L77, parent: BinaryResult)

> *Summary: Retrieves the raw byte data stored within the object instance. This method returns the internal `self.data` as a `bytes` object.*


### ModelResponse (class, L80-L125)

> *Summary: Represents the final output from a model interaction, encapsulating the message content, tool calls, usage statistics, and tracing details. It provides properties to easily access the response's content and metadata, and includes a method to serialize its core components into an API dictionary format.*


### metadata (method, L96-L97, parent: ModelResponse)

> *Summary: Retrieves the metadata dictionary associated with a message; returns an empty dictionary if no message is present on the instance.*


### content (method, L100-L101, parent: ModelResponse)

> *Summary: Retrieves the textual content from a message object, returning `None` if no message is present. This method provides direct access to the message's payload string.*


### __repr__ (method, L103-L116, parent: ModelResponse)

> *Summary: Generates a developer-friendly string representation of the object by including its content, optional metadata, tool calls, usage statistics, and file count. The output format is `ModelResponse(...)` containing details about these attributes.*


### to_api (method, L118-L125, parent: ModelResponse)

> *Summary: Converts the object's state into a dictionary format suitable for API consumption. It packages the message content and role, optionally including serialized tool calls if they exist.*


### ModelMessageChunk (class, L128-L137)

> *Summary: Represents a segment of streamed output from a language model. It holds a string containing part of the message content and is marked as transient, meaning it's not typically saved permanently.*


### HumanInputRequest (class, L140-L144)

> *Summary: Represents an event signaling the need for human interaction. It carries a unique ID and a string containing the content that requires user input.*


### HumanMessage (class, L147-L159)

> *Summary: Represents a user's input message, storing its content and an optional parent ID. It provides a class method to ensure the input is a `HumanMessage` instance, setting the required `parent_id` if it was not provided initially.*


### ensure_message (method, L154-L159, parent: HumanMessage)

> *Summary: This method ensures an input content is a `HumanMessage` instance, creating one if necessary using the provided class and content string. It then sets the message's `parent_id` if it was not already set, returning the finalized `HumanMessage`.*

