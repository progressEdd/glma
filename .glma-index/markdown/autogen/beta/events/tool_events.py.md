# autogen/beta/events/tool_events.py

11 class(es): ToolResult, ToolCallsEvent, ToolResultsEvent, ToolEvent, ToolCallEvent, ClientToolCallEvent, BuiltinToolCallEvent, ToolResultEvent, BuiltinToolResultEvent, ToolErrorEvent, ToolNotFoundEvent. 14 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| ToolResult | class |  |
| ToolCallsEvent | class |  |
| ToolResultsEvent | class |  |
| ToolEvent | class |  |
| ToolCallEvent | class |  |
| ClientToolCallEvent | class |  |
| BuiltinToolCallEvent | class |  |
| ToolResultEvent | class |  |
| BuiltinToolResultEvent | class |  |
| ToolErrorEvent | class |  |
| ToolNotFoundEvent | class |  |

## Chunks

### ToolResult (class, L20-L40)

> *Summary: Represents the outcome of a tool execution, encapsulating input parts, a final status flag, and associated metadata. It can be instantiated directly or created from various message types using a class method to ensure it conforms to the `ToolResult` structure.*


### __init__ (method, L25-L34, parent: ToolResult)

> *Summary: Initializes an event by processing a sequence of input messages and optional parts into a standardized list of inputs. It sets whether the event is terminal and stores any associated metadata.*


### ensure_result (method, L37-L40, parent: ToolResult)

> *Summary: This method ensures the input data conforms to a `ToolResult` type. If the provided data is already a `ToolResult`, it returns it directly; otherwise, it instantiates and returns a new `ToolResult` using the input data.*


### ToolCallsEvent (class, L43-L52)

> *Summary: This event aggregates multiple tool calls into a single container. It accepts a list of `ToolCallEvent` objects and provides methods to retrieve the count or convert all contained calls into a list of API-compatible dictionaries.*


### __len__ (method, L48-L49, parent: ToolCallsEvent)

> *Summary: Returns the total count of recorded calls within the object by checking the length of its internal `calls` list. This provides a quick way to determine how many actions have been executed.*


### to_api (method, L51-L52, parent: ToolCallsEvent)

> *Summary: Converts the internal structure of an event into a standardized API format by recursively transforming all contained call objects. It takes the object's state and outputs a list of dictionaries representing its components.*


### ToolResultsEvent (class, L55-L58)

> *Summary: This class serves as a container for collecting the outcomes, which can be successful results or errors, generated by external tools. It accepts a list of `ToolResultEvent` objects upon instantiation.*


### ToolEvent (class, L61-L62)

> *Summary: Serves as the foundational class for any event related to tool usage within the system. It inherits from `BaseEvent` and provides a common structure for specialized tool events.*


### ToolCallEvent (class, L65-L98)

> *Summary: This class models a request for the model to execute a specific tool. It takes a tool name and arguments (as a JSON string) as input, providing methods to serialize/deserialize the arguments and outputting a standardized dictionary format suitable for an API call.*


### serialized_arguments (method, L75-L78, parent: ToolCallEvent)

> *Summary: Retrieves the arguments of an event as a dictionary; if not already parsed and stored, it deserializes the raw `arguments` string using JSON.*


### serialized_arguments (method, L81-L82, parent: ToolCallEvent)

> *Summary: Stores a dictionary of arguments into an internal attribute. This method accepts one input argument, `value`, and has no return value.*


### __repr__ (method, L84-L88, parent: ToolCallEvent)

> *Summary: Provides a developer-friendly string representation of the event, including its ID and name. It conditionally appends a truncated representation of any associated arguments to the output.*


### to_api (method, L90-L98, parent: ToolCallEvent)

> *Summary: Converts the event object into a dictionary structure suitable for API transmission. It packages the event's ID, sets the type to "function," and includes the function name along with its serialized arguments as a JSON string.*


### ClientToolCallEvent (class, L101-L112)

> *Summary: Represents a client-side event signaling the invocation of a tool. It accepts a `ToolCallEvent` object as input and produces an instance containing the unique ID, tool name, and arguments string.*


### from_call (method, L107-L112, parent: ClientToolCallEvent)

> *Summary: Converts a `ToolCallEvent` instance into a client-facing event object by extracting its ID, name, and arguments. This method facilitates the translation of internal tool call events for external consumption.*


### BuiltinToolCallEvent (class, L115-L116)

> *Summary: This class inherits from `ToolCallEvent` to specifically represent when a language model requests the execution of a built-in tool. It serves as a structured event notification for such invocations.*


### ToolResultEvent (class, L119-L141)

> *Summary: This class models the outcome of a successful tool execution, holding references to the parent call ID, the tool name, and the resulting data. It provides a factory method to construct an instance from a `ToolCallEvent` and its corresponding result.*


### __repr__ (method, L127-L128, parent: ToolResultEvent)

> *Summary: Provides a developer-friendly string representation of the event object, displaying its class name along with its parent ID, name, and any associated result. This is useful for debugging and logging purposes.*


### from_call (method, L131-L136, parent: ToolResultEvent)

> *Summary: Constructs a `ToolResultEvent` from an existing `ToolCallEvent` and its execution result. It uses the call's ID and name to populate the new event while ensuring the provided result is correctly formatted as a `ToolResult`.*


### __eq__ (method, L138-L141, parent: ToolResultEvent)

> *Summary: Compares two `ToolResultEvent` instances for equality by checking if their respective parent IDs, names, and results match. Returns `True` only if all three attributes are identical across both objects.*


### BuiltinToolResultEvent (class, L144-L145)

> *Summary: This class inherits from `ToolResultEvent` to specifically represent the outcome of a successfully executed built-in tool. It serves as an event structure carrying the results back after a tool operation completes.*


### ToolErrorEvent (class, L148-L175)

> *Summary: This class models a failed tool execution, storing the associated `Exception`. It can be instantiated from a `ToolCallEvent` and an error object, formatting the traceback into the event's result payload. Equality is determined by matching parent ID, name, and the type and string representation of the contained error.*


### from_call (method, L154-L168, parent: ToolErrorEvent)

> *Summary: Constructs a `ToolErrorEvent` from a `ToolCallEvent` and an exception. It packages the error details, including a full traceback string, into the resulting event object.*


### __eq__ (method, L170-L175, parent: ToolErrorEvent)

> *Summary: Determines equality between two `ToolErrorEvent` instances by comparing their parent ID, name, and ensuring both events share the same error type and string representation of the error. It returns `False` if the comparison object is not also a `ToolErrorEvent`.*


### ToolNotFoundEvent (class, L178-L179)

> *Summary: This event signals that a requested tool could not be located. It inherits from `ToolErrorEvent` to represent a specific failure state during tool execution.*

