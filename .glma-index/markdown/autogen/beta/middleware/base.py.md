# autogen/beta/middleware/base.py

3 class(es): MiddlewareFactory, Middleware, BaseMiddleware. 8 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| MiddlewareFactory | class |  |
| Middleware | class |  |
| BaseMiddleware | class |  |

## Chunks

### MiddlewareFactory (class, L21-L22)

> *Summary: Defines a protocol for factories that accept an event and context to produce a middleware instance. This allows different implementations to conform to the expected factory interface.*


### __call__ (method, L22-L22, parent: MiddlewareFactory)

> *Summary: This method acts as the core handler for middleware, accepting a `BaseEvent` and a `Context` object. It processes the incoming event within the given context to determine the next step in the request pipeline.*


### Middleware (class, L25-L41)

> *Summary: This class acts as a factory wrapper to simplify middleware registration. It takes a middleware class and options upon initialization, then instantiates and returns that specific middleware instance when called with an event and context.*


### __init__ (method, L28-L34, parent: Middleware)

> *Summary: Initializes the middleware instance by storing a reference to its class and any provided configuration options. It accepts a middleware class type and arbitrary keyword arguments for setup.*


### __call__ (method, L36-L41, parent: Middleware)

> *Summary: This method acts as the middleware's entry point, taking an event and a context object as input. It delegates the actual processing to the class implementation using stored options, returning a new instance of the middleware.*


### BaseMiddleware (class, L54-L93)

> *Summary: This class provides a foundational structure for middleware by holding initial event and context data. It defines asynchronous hooks (`on_turn`, `on_tool_execution`, etc.) that wrap subsequent processing steps, allowing interception of various system events like turns, tool calls, LLM interactions, and human inputs.*


### __init__ (method, L55-L61, parent: BaseMiddleware)

> *Summary: Initializes the middleware by storing a reference to the incoming `BaseEvent` and its associated execution `Context`. This sets up the necessary state for subsequent processing within the middleware chain.*


### on_turn (method, L63-L69, parent: BaseMiddleware)

> *Summary: This method acts as a simple middleware wrapper that passes the incoming `event` and `context` to the next handler in the chain. It returns whatever response is generated by the subsequent function call.*


### on_tool_execution (method, L71-L77, parent: BaseMiddleware)

> *Summary: This method acts as a middleware wrapper that passes the incoming `ToolCallEvent` and `Context` to the next handler in the execution chain. It returns whatever result is produced by the subsequent tool execution logic.*


### on_llm_call (method, L79-L85, parent: BaseMiddleware)

> *Summary: This method acts as a pass-through middleware, executing the next function in the chain with the provided events and context. It returns the resulting `ModelResponse` from the subsequent handler.*


### on_human_input (method, L87-L93, parent: BaseMiddleware)

> *Summary: This method acts as a pass-through hook for human input requests. It accepts an event and context, then forwards them directly to the next handler in the chain, returning the resulting `HumanMessage`.*

