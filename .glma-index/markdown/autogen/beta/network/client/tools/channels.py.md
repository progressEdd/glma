# autogen/beta/network/client/tools/channels.py

2 function(s): _metadata_dict, make_channels_tool.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| _metadata_dict | function |  |
| make_channels_tool | function |  |

## Chunks

### _metadata_dict (function, L31-L55)

> *Summary: Converts a rich `metadata` object into a standardized dictionary representation suitable for serialization or external use. It extracts and structures various attributes like IDs, state, participant lists, knobs, labels, expectations, and timestamps from the input metadata.*


### make_channels_tool (function, L58-L163)

> *Summary: This function generates a tool that manages the lifecycle of communication channels via an `AgentClient`. It supports actions like listing active/all channels, opening new ones with optional seeding messages, retrieving channel information by ID, and closing specified or current channels.*

