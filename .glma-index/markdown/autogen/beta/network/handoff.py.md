# autogen/beta/network/handoff.py

1 class(es): Handoff.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| Handoff | class |  |

## Chunks

### Handoff (class, L25-L45)

> *Summary: Represents a routing intent returned by a tool that dynamically selects a target specialist based on an input query. It holds the `target` name and a descriptive `reason`, which the framework uses to set the active packet's routing information.*

