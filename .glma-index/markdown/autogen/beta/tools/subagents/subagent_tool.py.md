# autogen/beta/tools/subagents/subagent_tool.py

1 function(s): subagent_tool.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| subagent_tool | function |  |

## Chunks

### subagent_tool (function, L21-L53)

> *Summary: Creates a callable tool that delegates an objective to a specified agent. It accepts the agent object and configuration parameters like description and middleware, returning an asynchronous function that executes the task and returns its final string result.*

