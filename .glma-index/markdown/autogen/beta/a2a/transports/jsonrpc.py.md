# autogen/beta/a2a/transports/jsonrpc.py

1 function(s): build_jsonrpc_asgi.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| build_jsonrpc_asgi | function |  |

## Chunks

### build_jsonrpc_asgi (function, L27-L65)

> *Summary: Constructs a Starlette ASGI application that serves both JSON-RPC dispatch and agent card discovery endpoints. It takes various components like an `AgentExecutor`, configuration stores, and URL paths as inputs to build the routing structure. The output is a fully configured Starlette application instance ready for serving requests.*

