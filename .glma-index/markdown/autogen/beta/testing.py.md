# autogen/beta/testing.py

4 class(es): TestClient, TrackingClient, TrackingConfig, TestConfig. 12 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestClient | class |  |
| TrackingClient | class |  |
| TrackingConfig | class |  |
| TestConfig | class |  |

## Chunks

### TestClient (class, L21-L50)

> *Summary: This class simulates an LLM client by consuming a sequence of predefined events. It processes incoming messages, raises errors if any are `ToolErrorEvent`s, and returns the next simulated response based on whether the input event is a string (generating a message), an iterable of tool calls, or a single tool call event.*


### __init__ (method, L24-L25, parent: TestClient)

> *Summary: Initializes the object by accepting a variable number of events or a string as input and immediately converts them into an iterable sequence stored in `self.events`. This allows for sequential processing of incoming event data.*


### __call__ (method, L27-L50, parent: TestClient)

> *Summary: Processes a sequence of input events to determine the next action based on internal state. It checks for tool errors in the input messages and then transforms the next event—whether it's a string, iterable, or `ToolCallEvent`—into a structured `ModelResponse`.*


### TrackingClient (class, L53-L65)

> *Summary: This wrapper intercepts calls to an underlying `LLMClient` by first invoking a provided mock object with the last message from the input sequence. It then passes the full messages and context through to the original client to return the final model response.*


### __init__ (method, L54-L56, parent: TrackingClient)

> *Summary: Initializes the testing object by storing a provided `LLMClient` instance and a `MagicMock` object for dependency injection. These attributes are used throughout the test suite to control external service interactions.*


### __call__ (method, L58-L65, parent: TrackingClient)

> *Summary: This method intercepts incoming messages by mocking the last one before forwarding the entire sequence of events and context to an underlying client for processing. It returns a `ModelResponse` generated by that client call.*


### TrackingConfig (class, L68-L80)

> *Summary: This class wraps a `ModelConfig` and provides methods to instantiate tracking clients. It allows creating a standard tracking client using the underlying configuration while mocking external dependencies, but explicitly disallows file-related API creation.*


### __init__ (method, L69-L71, parent: TrackingConfig)

> *Summary: Initializes the object by storing a `ModelConfig` instance and creating a mock object for testing purposes.*


### copy (method, L73-L74, parent: TrackingConfig)

> *Summary: Returns a reference to the current instance, effectively creating a shallow copy of the object. This method allows for chaining operations on the object.*


### create (method, L76-L77, parent: TrackingConfig)

> *Summary: Instantiates and returns a `TrackingClient` object, using configuration data from the instance's config and a provided mock object as inputs. This method is responsible for setting up the tracking client based on existing setup parameters.*


### create_files_client (method, L79-L80, parent: TrackingConfig)

> *Summary: This method raises a `NotImplementedError` if the current object type lacks support for the Files API. It serves as a placeholder to enforce implementation in subclasses that handle file operations.*


### TestConfig (class, L83-L96)

> *Summary: This configuration object holds a collection of events, which can be individual responses, tool calls, or an iterable thereof. It provides methods to create a standard test client from its stored events and explicitly disallows file-related client creation.*


### __init__ (method, L86-L87, parent: TestConfig)

> *Summary: Initializes the object by storing a variable number of input arguments, which can be model responses, tool call events, iterables of tool calls, or strings. This stored collection is assigned to the `self.events` attribute for later use.*


### copy (method, L89-L90, parent: TestConfig)

> *Summary: Returns a reference to the current instance, effectively creating a shallow copy of the object. This method allows for chaining operations on the object without modification.*


### create (method, L92-L93, parent: TestConfig)

> *Summary: Instantiates and returns a `TestClient` object, passing all stored events from the instance as arguments to its constructor.*


### create_files_client (method, L95-L96, parent: TestConfig)

> *Summary: This method raises a `NotImplementedError` if the current object type does not support the Files API. It serves as a placeholder to enforce implementation in subclasses that handle file operations.*

