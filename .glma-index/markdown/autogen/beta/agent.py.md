# autogen/beta/agent.py

6 function(s): _get_stream_turn_lock, _execute_turn, _wrap_prompt_hook, _build_subtask_tools, _filter_subtask_tools, _make_knowledge_tool. 12 class(es): KnowledgeConfig, TaskConfig, AgentReply, Agent, Plugin, _HaltCheckMiddleware, _HaltCheckMiddlewareFactory, _AssemblerMiddlewareFactory, _CompactionMiddleware, _CompactionMiddlewareFactory, _AggregationMiddleware, _AggregationMiddlewareFactory. 70 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| KnowledgeConfig | class |  |
| TaskConfig | class |  |
| AgentReply | class |  |
| _get_stream_turn_lock | function |  |
| Agent | class |  |
| _execute_turn | function |  |
| _wrap_prompt_hook | function |  |
| _build_subtask_tools | function |  |
| _filter_subtask_tools | function |  |
| _make_knowledge_tool | function |  |
| Plugin | class |  |
| _HaltCheckMiddleware | class |  |
| _HaltCheckMiddlewareFactory | class |  |
| _AssemblerMiddlewareFactory | class |  |
| _CompactionMiddleware | class |  |
| _CompactionMiddlewareFactory | class |  |
| _AggregationMiddleware | class |  |
| _AggregationMiddlewareFactory | class |  |

## Chunks

### KnowledgeConfig (class, L106-L143)

> *Summary: This configuration object groups parameters for an agent's knowledge management. It holds a backing `KnowledgeStore` and controls features like whether the LLM can interact with the store via tools, if stream history is logged, and optional strategies for data compaction or aggregation.*


### TaskConfig (class, L147-L164)

> *Summary: This class aggregates configuration parameters for task-spawning agents, defining the model to use, default prompt, and toolsets. It allows developers to explicitly control which tools are available to a subtask agent via inclusion/exclusion lists or by adding extra capabilities.*


### AgentReply (class, L167-L333)

> *Summary: This class encapsulates an agent's response, holding the model output and context. It provides a method to iteratively validate the response against a schema, automatically re-prompting the agent with validation errors until successful or maximum retries are reached.*


### __init__ (method, L168-L183, parent: AgentReply)

> *Summary: Initializes an object by storing a model response, execution context, LLM client, and references to other agents and providers. It also optionally accepts a schema for the expected output result.*


### content (method, L185-L224, parent: AgentReply)

> *Summary: This method iteratively attempts to validate the agent's current body against a predefined schema. If validation fails and retries remain, it prompts the agent with the error details and schema for correction before trying again.*


### body (method, L227-L229, parent: AgentReply)

> *Summary: Retrieves the textual content from the agent's stored response object. It returns this string content or `None` if it is unavailable.*


### files (method, L232-L234, parent: AgentReply)

> *Summary: Retrieves a list of `BinaryResult` objects representing images that were generated by the model during the current interaction. This method accesses and returns the stored file data from the agent's response object.*


### history (method, L237-L238, parent: AgentReply)

> *Summary: Retrieves the complete interaction log from the agent's context stream. This method returns a `History` object containing all past messages and turns.*


### ask (method, L241-L253, parent: AgentReply)

> *Summary: This asynchronous method executes a query by taking various inputs like messages, configuration, tools, and middleware. It returns an `AgentReply` object structured according to the provided response schema.*


### ask (method, L256-L268, parent: AgentReply)

> *Summary: This asynchronous method executes a query by taking various inputs like messages, configuration, tools, and middleware. It returns an `AgentReply` object containing the result of the agent's processing based on these inputs.*


### ask (method, L271-L283, parent: AgentReply)

> *Summary: This asynchronous method executes a query by taking various inputs like messages, configuration, tools, and middleware. It returns an `AgentReply` object containing the agent's response based on the provided context and instructions.*


### ask (method, L286-L297, parent: AgentReply)

> *Summary: This asynchronous method initiates an interaction by accepting messages, configuration parameters like tools and prompts, and various optional hooks. It returns a structured reply object containing the agent's response to the input.*


### ask (method, L299-L333, parent: AgentReply)

> *Summary: This method executes an agent's request by constructing a `ModelRequest` from input messages and merging provided dependencies, variables, and prompts into the agent's context. It then delegates the execution to an internal method using configured clients, tools, middleware, and observers to return an `AgentReply`.*


### _get_stream_turn_lock (function, L339-L363)

> *Summary: Retrieves or creates an `asyncio.Lock` associated with a given stream object to serialize concurrent access across turns. It lazily attaches the lock directly to the stream, falling back to a global registry if the stream uses slots and prevents attribute setting.*


### Agent (class, L373-L1211)

> *Summary: This class serves as the core agentic unit, managing an LLM execution loop that supports optional features like tool invocation, middleware chaining, event observation, and knowledge persistence. It accepts various configurations for prompts, tools, policies, and external services to execute a request via its `ask` method, returning an `AgentReply`.*


### __init__ (method, L395-L412, parent: Agent)

> *Summary: Initializes an agent by accepting configuration for its identity, prompts, model settings, available tools, and various operational hooks. It sets up the agent's core structure using provided schemas, plugins, knowledge bases, and task definitions.*


### __init__ (method, L415-L432, parent: Agent)

> *Summary: Initializes an agent by accepting configuration for its identity, prompting behavior (single or multiple prompts), model settings, available tools, middleware, and observers. It also configures response structure, knowledge sources, task execution parameters, and dependencies to define the agent's operational scope.*


### __init__ (method, L435-L452, parent: Agent)

> *Summary: Initializes an agent by accepting configuration for its identity, prompts, model settings, available tools, and various operational hooks like middleware and observers. It sets up the agent's core structure using provided schemas, knowledge bases, and task definitions.*


### __init__ (method, L455-L472, parent: Agent)

> *Summary: Initializes an agent by accepting configuration for its identity, prompts, model settings, available tools, and various operational hooks like middleware and observers. It sets up the agent's core behavior based on provided dependencies, knowledge sources, and task definitions.*


### __init__ (method, L474-L563, parent: Agent)

> *Summary: Initializes an agent by configuring its identity, system prompts, available tools, middleware, and dependencies. It sets up internal components like serializers, tool executors, and knowledge management structures based on provided configurations.*


### hitl_hook (method, L565-L574, parent: Agent)

> *Summary: Wraps a provided `HumanHook` instance to inject custom logic before returning the original hook. It issues a warning if an existing HITL hook is being overwritten.*


### prompt (method, L577-L580, parent: Agent)

> *Summary: This method returns a callable that modifies a `PromptHook` based on an optional function provided as input. It allows for dynamic alteration of the prompting behavior during agent execution.*


### prompt (method, L583-L586, parent: Agent)

> *Summary: This method takes a `PromptHook` object as input and returns a modified `PromptHook`. Its purpose is to allow the agent to interact with or modify prompting behavior via the provided hook.*


### prompt (method, L588-L598, parent: Agent)

> *Summary: This method returns a decorator or the original hook, depending on whether an input function is provided. It wraps the given prompt hook by appending it to the agent's dynamic prompt list before returning the wrapped version.*


### tool (method, L601-L610, parent: Agent)

> *Summary: Registers a callable function as an available tool for the agent. It accepts the function itself along with optional metadata like name, description, and schema, and controls execution behavior via threading and middleware.*


### tool (method, L613-L622, parent: Agent)

> *Summary: This method wraps a given function to create a `Tool` object. It accepts optional parameters like name, description, and schema for the tool's definition, and allows configuration of execution behavior via middleware and threading.*


### add_middleware (method, L624-L631, parent: Agent)

> *Summary: Appends a new `MiddlewareFactory` instance to the agent's internal middleware list, ensuring it executes last upon turn entry and first upon turn exit relative to existing layers. This method returns the agent instance for chaining operations.*


### insert_middleware (method, L633-L640, parent: Agent)

> *Summary: This method prepends a new `MiddlewareFactory` to the agent's existing middleware chain. It ensures the provided middleware executes first upon entering and last upon exiting any turn cycle.*


### add_tool (method, L642-L644, parent: Agent)

> *Summary: Registers a callable or `Tool` object with the agent's toolset after ensuring it conforms to the expected `FunctionTool` structure using the agent's dependency provider. It returns the modified agent instance for chaining operations.*


### add_observer (method, L646-L648, parent: Agent)

> *Summary: Registers a provided `Observer` object to the agent's internal list before any interaction begins. This allows the agent to notify the observer of relevant events during its operation.*


### add_policy (method, L650-L660, parent: Agent)

> *Summary: Appends a specified `AssemblyPolicy` to the agent's internal list of policies, ensuring new policies execute after existing ones. It returns the modified agent instance for method chaining.*


### task (method, L662-L700, parent: Agent)

> *Summary: This method constructs and returns a `Task` object owned by the agent, accepting parameters like title, description, payload, and optional context or time-to-live. It prepares the task specification for lifecycle management, allowing it to be used within an asynchronous context manager where its execution is tracked.*


### tool (method, L702-L727, parent: Agent)

> *Summary: This method acts as a factory for creating and registering tools with the agent. It accepts optional configuration like name, description, and schema, returning either a wrapped tool instance or a function that creates and registers such a tool when called.*


### observer (method, L730-L734, parent: Agent)

> *Summary: Registers a callback function to be executed when a specified condition or class information is met. It accepts the target condition/class and the execution handler as inputs, returning the registered callable.*


### observer (method, L737-L740, parent: Agent)

> *Summary: This method returns a decorator that wraps functions to observe their execution based on an optional `ClassInfo` or `Condition`. It allows monitoring the behavior of decorated methods during agent operations.*


### observer (method, L742-L754, parent: Agent)

> *Summary: This method registers a function to be observed based on a specified condition and optionally wraps the provided callback with an observer factory. It returns either the wrapped callback or a wrapper function that accepts and then wraps another callable.*


### ask (method, L757-L770, parent: Agent)

> *Summary: This asynchronous method executes a query by processing input messages and optional configurations like tools, middleware, and observers. It returns an `AgentReply` object containing the structured result based on the provided response schema.*


### ask (method, L773-L787, parent: Agent)

> *Summary: This asynchronous method sends a message or input to the agent, optionally streaming the response and incorporating dependencies, variables, tools, and middleware. It returns an `AgentReply` object containing the final result based on the provided response schema.*


### ask (method, L790-L804, parent: Agent)

> *Summary: This asynchronous method sends a message or input to the agent, optionally streaming the response and allowing configuration via dependencies, variables, tools, middleware, and observers. It returns an `AgentReply` object containing the final result based on the provided inputs and settings.*


### ask (method, L807-L820, parent: Agent)

> *Summary: This asynchronous method sends a message or input to the agent, optionally streaming the response and allowing configuration via various inputs like tools, middleware, and observers. It returns an `AgentReply` object containing the final result of the interaction.*


### ask (method, L822-L869, parent: Agent)

> *Summary: This method initiates an interaction by constructing a request context from input messages and optional configurations. It then executes the core logic via `_execute`, returning an agent reply after potentially augmenting the prompt with system or dynamic content.*


### _ask (method, L871-L906, parent: Agent)

> *Summary: This method executes an agent request using a pre-built context. It constructs the initial prompt by combining any existing context with system and dynamic prompts before passing everything to the core execution logic.*


### _build_knowledge_tool (method, L908-L910, parent: Agent)

> *Summary: Retrieves a pre-computed list of available tools, which are designed to access external knowledge sources. This method returns the internal `_knowledge_tools` attribute without performing any computation.*


### _build_subtask_tools (method, L912-L919, parent: Agent)

> *Summary: Retrieves a pre-computed list of tools associated with subtasks, which is cached during agent initialization to ensure efficient, single-time construction. This method returns the internal `self._subtask_tools` list without recalculating the tool definitions on each call.*


### _spawn_subtask (method, L921-L950, parent: Agent)

> *Summary: Creates and executes a new, isolated agent instance to handle a specific subtask. This child agent inherits filtered tools from the parent but is explicitly prevented from spawning further subtasks, returning its final result or an error string.*


### _execute (method, L952-L991, parent: Agent)

> *Summary: This method ensures sequential execution of agent turns on a shared stream by acquiring a per-stream lock. It takes an event and various configuration objects as input to process the turn, ultimately returning an `AgentReply`. This locking mechanism prevents race conditions like duplicate tool executions when multiple asynchronous calls share the same communication stream.*


### _execute_locked (method, L993-L1188, parent: Agent)

> *Summary: Executes an agent's turn by orchestrating tool execution, LLM interaction, and middleware processing based on provided event data. It manages knowledge store bootstrapping, constructs a chain of middlewares (including assembly and compaction logic), and handles lifecycle events for observers before returning the final agent reply.*


### as_tool (method, L1190-L1204, parent: Agent)

> *Summary: Converts the current agent instance into a `FunctionTool` by wrapping it with provided metadata. It accepts a description and optional name, streaming mechanism, and middleware to define the tool's behavior.*


### as_conversable (method, L1206-L1211, parent: Agent)

> *Summary: This method wraps the agent instance into an adapter object that conforms to the `ConversableAdapter` interface. It returns this wrapper, allowing the agent's functionality to be used within a conversational context.*


### _execute_turn (function, L1214-L1224)

> *Summary: This function processes a single interaction turn by first sending an initial event to the model and awaiting its response. It then iteratively sends any detected tool calls from the model's response until all tools have been executed or the response is forced, returning the final aggregated `ModelResponse`.*


### _wrap_prompt_hook (function, L1227-L1243)

> *Summary: This function wraps a `PromptHook` to create an asynchronous callable that executes the hook logic against a model request and context. It returns a wrapper function which handles execution using an exit stack for resource management, ultimately yielding the resulting string from the hooked model call.*


### _build_subtask_tools (function, L1264-L1296)

> *Summary: Generates a list of callable tools (`run_subtask` and `run_subtasks`) for an agent instance. These tools allow the agent to execute single or multiple subtasks asynchronously, returning formatted results based on whether parallel execution is requested.*


### _filter_subtask_tools (function, L1299-L1320)

> *Summary: Filters a collection of available tools based on provided inclusion and exclusion lists. It iterates through the input tools, keeping only those whose names are either not explicitly excluded or match the specified allowlist criteria.*


### _make_knowledge_tool (function, L1323-L1368)

> *Summary: Creates a callable tool that interfaces with a `KnowledgeStore` to allow an LLM agent to perform file operations like reading, writing, listing, and deleting data within the store. It returns this single, stable tool definition for use across multiple conversational turns.*


### Plugin (class, L1371-L1534)

> *Summary: This class acts as a modular extension mechanism for an Agent, allowing users to inject custom tools, middleware, prompts, observers, and dependencies. It accepts various configurations during initialization and provides methods like `register` to apply these contributions to an existing Agent instance.*


### __init__ (method, L1372-L1399, parent: Plugin)

> *Summary: Initializes an agent by accepting configuration for prompts (static or dynamic), tools, middleware, observers, and dependency/variable mappings. It processes the input `prompt` iterable to populate internal lists with system-level strings and callable functions for dynamic prompt generation.*


### register (method, L1401-L1423, parent: Plugin)

> *Summary: This method integrates a plugin's capabilities into an existing agent instance by adding its tools and middleware. It also merges dependencies, variables, observers, and prompt components while conditionally setting the agent's HITL hook if one isn't already present.*


### hitl_hook (method, L1425-L1433, parent: Plugin)

> *Summary: Sets a human-in-the-loop callback function if one hasn't been previously configured. It accepts a `HumanHook` object as input and returns it, issuing a warning if an existing hook is overwritten.*


### prompt (method, L1436-L1439, parent: Plugin)

> *Summary: This method takes a `PromptHook` object as input and returns a modified `PromptHook`. Its purpose is to allow the agent to interact with or modify prompting behavior via the provided hook.*


### prompt (method, L1442-L1445, parent: Plugin)

> *Summary: This method accepts an optional function and returns a callable that modifies a `PromptHook`. It is designed to inject or alter prompts based on the provided function.*


### prompt (method, L1447-L1457, parent: Plugin)

> *Summary: This method returns a decorator or the original hook, depending on whether an input function is provided. If a function is given, it wraps that function to dynamically append its prompt hook to the agent's internal list before returning the wrapped version.*


### tool (method, L1460-L1469, parent: Plugin)

> *Summary: Creates a structured tool object from a given callable function. It accepts the function itself along with optional metadata like name, description, and schema, and configures execution behavior via threading and middleware.*


### tool (method, L1472-L1481, parent: Plugin)

> *Summary: This method wraps a given function to create a `FunctionTool` object. It accepts optional parameters like name, description, and schema for the tool's metadata, and allows configuration of execution behavior via middleware.*


### tool (method, L1483-L1507, parent: Plugin)

> *Summary: This method acts as a factory for creating and registering callable tools with the agent's internal tool registry. It accepts optional configuration like name, description, and schema to wrap a provided function, returning either the wrapped `FunctionTool` or a higher-order function that creates it upon invocation.*


### observer (method, L1510-L1514, parent: Plugin)

> *Summary: This method registers a callback function to be executed when a specified condition is met. It accepts the target condition and the callable to execute upon its fulfillment as inputs, returning the registered callback itself.*


### observer (method, L1517-L1520, parent: Plugin)

> *Summary: This method returns a decorator that wraps functions to observe their execution based on an optional `Condition` or `ClassInfo`. It allows monitoring the behavior of wrapped callables during agent operations.*


### observer (method, L1522-L1534, parent: Plugin)

> *Summary: This method registers a function to be observed based on a specified condition and optionally executes a callback upon observation. It returns either the wrapped original function or a wrapper that applies the observation logic before executing the provided callback.*


### _HaltCheckMiddleware (class, L1537-L1582)

> *Summary: This middleware intercepts stream events to detect a `HaltEvent` from an `AlertPolicy`. If a halt is detected during the turn, subsequent LLM calls are short-circuited, returning a synthetic "HALTED" response instead of invoking the model.*


### __init__ (method, L1547-L1550, parent: _HaltCheckMiddleware)

> *Summary: Initializes an agent instance by accepting a `BaseEvent` and a `Context`. It sets internal state flags to indicate the agent is not halted and has no halt reason initially.*


### on_turn (method, L1552-L1568, parent: _HaltCheckMiddleware)

> *Summary: This method intercepts an event stream to check for a `HaltEvent`, setting internal flags if one is received. It then executes the next step in the processing chain and ensures the subscription is cleaned up afterward.*


### on_llm_call (method, L1570-L1582, parent: _HaltCheckMiddleware)

> *Summary: If the agent is halted, it immediately returns a response indicating the halt reason; otherwise, it proceeds by calling the next function in the execution chain with the provided events and context.*


### _HaltCheckMiddlewareFactory (class, L1585-L1589)

> *Summary: This factory creates an instance of `_HaltCheckMiddleware` by accepting a `BaseEvent` and a `Context`. Its purpose is to provide the middleware object needed for event processing.*


### __call__ (method, L1588-L1589, parent: _HaltCheckMiddlewareFactory)

> *Summary: When invoked with an `event` and `context`, this method returns a middleware object that checks for halting conditions based on the provided inputs. This acts as a gatekeeper to control the flow of execution within the system.*


### _AssemblerMiddlewareFactory (class, L1592-L1599)

> *Summary: This factory creates instances of `AssemblerMiddleware` by accepting a list of `AssemblyPolicy` objects during initialization. When called with an event and context, it returns the configured middleware instance.*


### __init__ (method, L1595-L1596, parent: _AssemblerMiddlewareFactory)

> *Summary: Initializes the agent by storing a list of `AssemblyPolicy` objects. This method accepts one input parameter, `policies`, and stores it internally for later use.*


### __call__ (method, L1598-L1599, parent: _AssemblerMiddlewareFactory)

> *Summary: When invoked with a `BaseEvent` and `Context`, this method returns an `AssemblerMiddleware` instance, encapsulating the agent's defined policies.*


### _CompactionMiddleware (class, L1602-L1687)

> *Summary: This middleware intercepts agent turns to conditionally trigger data compaction based on predefined event count or token thresholds. It processes the conversation history, executes a specified compaction strategy if limits are exceeded, and then updates the history with the compacted events, reporting start/completion status via context messages.*


### __init__ (method, L1605-L1620, parent: _CompactionMiddleware)

> *Summary: Initializes an agent instance by setting its core components: the event, context, specific actor name, operational strategy, optional knowledge store, and activation trigger. It stores these inputs internally to govern the agent's behavior and state tracking.*


### on_turn (method, L1622-L1687, parent: _CompactionMiddleware)

> *Summary: After processing an event, this method checks the conversation history against configured limits (max events or max tokens). If thresholds are exceeded and compaction is triggered, it executes a strategy to compact the history, notifies the context of the process, updates internal state, and returns the original result.*


### _CompactionMiddlewareFactory (class, L1690-L1713)

> *Summary: This factory constructs and returns an instance of `_CompactionMiddleware` based on provided configuration parameters. It takes actor name, compaction strategy, knowledge store, and a trigger as input to build the middleware object for processing events within a given context.*


### __init__ (method, L1693-L1703, parent: _CompactionMiddlewareFactory)

> *Summary: Initializes an agent instance by setting its name, a defined execution strategy, optional knowledge storage, and a specific activation trigger. These parameters define the agent's identity, behavior logic, memory access, and operational conditions.*


### __call__ (method, L1705-L1713, parent: _CompactionMiddlewareFactory)

> *Summary: This method constructs and returns a `_CompactionMiddleware` instance. It takes an incoming event and context as input to wrap the agent's logic with compaction behavior using its internal configuration like actor name and strategy.*


### _AggregationMiddleware (class, L1716-L1811)

> *Summary: This middleware intercepts agent turns to trigger aggregation based on configured thresholds. It checks for conditions like turn counts or total event history crossing multiples of a set value, then executes the specified aggregation strategy using the stream's historical events.*


### __init__ (method, L1732-L1746, parent: _AggregationMiddleware)

> *Summary: Initializes an agent instance by setting its core components: a name, aggregation strategy, knowledge store, and event trigger. It accepts these dependencies along with the base event and context for operation.*


### on_turn (method, L1748-L1811, parent: _AggregationMiddleware)

> *Summary: This method intercepts the execution flow after a turn completes to determine if aggregation should occur based on configured triggers (end of process, fixed turn count, or event count thresholds). If triggered, it executes an aggregation strategy against the collected events and reports the start, completion, or failure of this process via context events.*


### _AggregationMiddlewareFactory (class, L1814-L1837)

> *Summary: This factory constructs and returns an `_AggregationMiddleware` instance. It takes configuration parameters—including actor name, aggregation strategy, knowledge store, and trigger—to initialize the middleware for processing events within a given context.*


### __init__ (method, L1817-L1827, parent: _AggregationMiddlewareFactory)

> *Summary: Initializes an agent instance by storing its designated name, a specific aggregation strategy, a knowledge base for storage, and a mechanism to trigger actions. These inputs define the agent's identity and operational parameters.*


### __call__ (method, L1829-L1837, parent: _AggregationMiddlewareFactory)

> *Summary: When invoked with an event and context, this method constructs and returns an `AggregationMiddleware` instance. This middleware encapsulates the agent's configuration, including its name, strategy, storage mechanism, and trigger logic.*

