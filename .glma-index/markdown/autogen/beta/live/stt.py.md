# autogen/beta/live/stt.py

4 class(es): VoiceInput, STTConfig, VoicePipeline, VoiceReply. 11 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| VoiceInput | class |  |
| STTConfig | class |  |
| VoicePipeline | class |  |
| VoiceReply | class |  |

## Chunks

### VoiceInput (class, L24-L27)

> *Summary: This class holds the raw audio data, specified as 16-bit PCM bytes, along with its corresponding sample rate and channel count. It serves to encapsulate the necessary metadata for processing incoming voice input streams.*


### STTConfig (class, L30-L35)

> *Summary: Defines an abstract configuration for speech-to-text operations, requiring implementations to asynchronously transcribe a `VoiceInput` using provided `Context`. It also provides a method to construct and return a `VoicePipeline` instance tied to an agent.*


### transcribe (method, L32-L32, parent: STTConfig)

> *Summary: This asynchronous method takes a `VoiceInput` and `Context` to convert audio into text. It returns the resulting transcribed string.*


### pipe (method, L34-L35, parent: STTConfig)

> *Summary: Creates and returns a `VoicePipeline` instance, initializing it with the provided `Agent` and the current object's context. This method acts as a factory to establish the voice processing pipeline for an agent.*


### VoicePipeline (class, L38-L76)

> *Summary: This component processes voice input by first transcribing the audio using a provided configuration and context. It then passes the resulting text to an agent for response generation, ultimately returning a structured `VoiceReply`.*


### __init__ (method, L39-L41, parent: VoicePipeline)

> *Summary: Initializes the object by storing a reference to an `Agent` instance and its associated `STTConfig`. This sets up the necessary components for speech-to-text processing operations.*


### ask (method, L43-L76, parent: VoicePipeline)

> *Summary: This method processes voice input by first transcribing the `VoiceInput` into text using an STT configuration. It then passes this transcribed text, along with various context and configuration parameters, to an underlying agent's ask function to generate a reply, which is finally wrapped in a `VoiceReply`.*


### VoiceReply (class, L79-L137)

> *Summary: Wraps an `AgentReply` and `STTConfig` to facilitate voice interaction. It uses the provided STT configuration to transcribe a `VoiceInput` into text before passing that text to the underlying reply object for processing. Accessors provide direct access to the reply's content, response metadata, files, history, and allow initiating an asynchronous query (`ask`) with various execution parameters.*


### __init__ (method, L85-L87, parent: VoiceReply)

> *Summary: Initializes the object by storing a required `AgentReply` object and an optional `STTConfig` object as instance attributes. These inputs define the context for subsequent speech-to-text operations.*


### content (method, L89-L94, parent: VoiceReply)

> *Summary: This method forwards a request to the underlying reply object's `content` function, allowing for optional retry configuration. It returns the resulting string content or `None`.*


### response (method, L97-L98, parent: VoiceReply)

> *Summary: Returns the `ModelResponse` object stored in the instance's `reply` attribute. This method provides direct access to the final model output after a request has been processed.*


### body (method, L101-L103, parent: VoiceReply)

> *Summary: Retrieves the text content of the model's reply from the instance's `reply` attribute, returning it as a string or `None`. This method provides the primary textual output for the current interaction turn.*


### files (method, L106-L108, parent: VoiceReply)

> *Summary: Retrieves a list of `BinaryResult` objects representing images that were generated by the model during the current interaction turn. This method accesses and returns the stored file data from the instance's reply object.*


### history (method, L111-L112, parent: VoiceReply)

> *Summary: Retrieves the complete message exchange history associated with the current reply object. It returns an instance of the `History` type containing all prior interactions.*


### ask (method, L114-L137, parent: VoiceReply)

> *Summary: This method takes voice input and configuration parameters to transcribe the audio using an internal STT service. It then passes the resulting text to a reply mechanism to generate and return an agent response.*

