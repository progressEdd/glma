# autogen/agentchat/contrib/capabilities/vision_capability.py

1 class(es): VisionCapability. 4 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| VisionCapability | class |  |

## Chunks

### VisionCapability (class, L29-L215)

> *Summary: This class intercepts incoming messages to augment them with descriptions for any embedded images before they reach the agent's core logic. It uses an LMM client or a custom function to generate captions from image URLs, transforming the input into a string format that includes these textual descriptions alongside the original content.*


### __init__ (method, L48-L94, parent: VisionCapability)

> *Summary: Sets up the vision capability by configuring an LMM client using provided settings and optional prompts/custom functions. It validates that either LMM configuration or a custom captioning function is supplied to enable image processing capabilities.*


### add_to_agent (method, L96-L103, parent: VisionCapability)

> *Summary: This method configures an agent by setting it as the parent and appending a system message informing it about image interpretation capabilities. It also registers a specific hook to process the agent's most recent incoming message.*


### process_last_received_message (method, L105-L188, parent: VisionCapability)

> *Summary: Transforms incoming message content, which can be a string or a list of structured items, into an augmented string suitable for GPT-4V. It processes text directly and fetches/generates captions for any embedded image URLs, appending these descriptions to the final output.*


### _get_image_caption (method, L190-L215, parent: VisionCapability)

> *Summary: This method takes base64 encoded image data as input and sends it to an LLM client along with a predefined prompt. It returns the resulting text caption generated by the language model for the provided image.*

