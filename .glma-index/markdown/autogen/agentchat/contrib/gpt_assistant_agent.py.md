# autogen/agentchat/contrib/gpt_assistant_agent.py

1 class(es): GPTAssistantAgent. 18 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| GPTAssistantAgent | class |  |

## Chunks

### GPTAssistantAgent (class, L28-L543)

> *Summary: This deprecated agent leverages the OpenAI Assistant API to manage conversational state instead of relying on internal memory. It initializes by either creating a new assistant or retrieving an existing one based on provided configurations, and it processes user input by creating and running threads against the configured OpenAI assistant.*


### __init__ (method, L39-L198, parent: GPTAssistantAgent)

> *Summary: Initializes an agent wrapper for interacting with OpenAI Assistants, configuring it based on provided instructions, LLM settings, and assistant configurations. It either retrieves an existing assistant by ID or name/instructions, creating a new one if necessary, while managing instruction and tool overwrites according to specified flags.*


### _invoke_assistant (method, L200-L262, parent: GPTAssistantAgent)

> *Summary: This method interacts with the OpenAI API to generate a reply from an assistant based on a conversation history. It first ensures an active thread exists for the sender, then posts all unread messages into that thread before initiating a run and aggregating the resulting responses. The function returns `True` along with the structured response content generated by the AI.*


### _map_role_for_api (method, L264-L283, parent: GPTAssistantAgent)

> *Summary: Transforms internal message roles into the specific string format required by the OpenAI Assistant API. It maps custom or tool-related roles like `"function"` and `"tool"` to `"assistant"`, while preserving standard roles like `"system"` and `"user"`.*


### _get_run_response (method, L285-L349, parent: GPTAssistantAgent)

> *Summary: This method polls an OpenAI assistant run until it completes or requires action. If completed, it retrieves and formats all relevant messages from the thread; otherwise, if it needs action, it executes necessary tools and submits the results back to continue the run.*


### _wait_for_run (method, L351-L367, parent: GPTAssistantAgent)

> *Summary: This method polls the OpenAI API using a provided `run_id` and `thread_id` until the associated run reaches a terminal state. It pauses between checks based on a configured interval, ultimately returning the final state of the run object.*


### _format_assistant_message (method, L369-L396, parent: GPTAssistantAgent)

> *Summary: This method takes a message object and modifies its content by replacing annotated text with numbered footnote markers. It then retrieves associated file information via the OpenAI client to construct and append detailed citations at the end of the message string.*


### can_execute_function (method, L398-L400, parent: GPTAssistantAgent)

> *Summary: This method checks if the agent is capable of running a specified function by returning `False` unconditionally. It takes a function name as input and returns a boolean indicating execution capability.*


### reset (method, L402-L410, parent: GPTAssistantAgent)

> *Summary: Clears all active OpenAI chat threads by deleting them and resets internal state tracking for conversation history. This method ensures a completely fresh start for subsequent interactions with the agent.*


### clear_history (method, L412-L425, parent: GPTAssistantAgent)

> *Summary: This method removes the chat history for a specified agent, or all agents if none is provided. It deletes the corresponding OpenAI thread and resets tracking indices to ensure a fresh conversation state.*


### pretty_print_thread (method, L427-L449, parent: GPTAssistantAgent)

> *Summary: Retrieves and displays the contents of a specified thread by fetching all messages from the OpenAI client. It sorts these messages chronologically and prints each message's timestamp, role, content types, and detailed text or image file IDs.*


### oai_threads (method, L452-L454, parent: GPTAssistantAgent)

> *Summary: Retrieves and returns the internal list of `Agent` objects associated with OpenAI threads. This method provides direct access to the agent's thread history or participants.*


### assistant_id (method, L457-L459, parent: GPTAssistantAgent)

> *Summary: Retrieves and returns the unique identifier of the associated OpenAI assistant object. This method accesses the internal `_openai_assistant` attribute to provide the ID string.*


### openai_client (method, L462-L463, parent: GPTAssistantAgent)

> *Summary: Returns the initialized OpenAI client instance held by the object. This method provides access to the configured API interface for external calls.*


### openai_assistant (method, L466-L467, parent: GPTAssistantAgent)

> *Summary: Returns the internal `_openai_assistant` object, which represents a preconfigured assistant agent utilizing OpenAI's capabilities. This method provides direct access to the initialized assistant instance.*


### get_assistant_instructions (method, L469-L471, parent: GPTAssistantAgent)

> *Summary: Retrieves the specific instructions configured for the AI assistant by accessing the internal `_openai_assistant` object's instruction attribute. This method returns a string containing the prompt or guidelines given to the assistant model.*


### delete_assistant (method, L473-L476, parent: GPTAssistantAgent)

> *Summary: Removes a configured AI assistant by calling the OpenAI Assistants API using the stored `assistant_id`. This action permanently deletes the assistant resource from the external service.*


### find_matching_assistant (method, L478-L524, parent: GPTAssistantAgent)

> *Summary: Filters a list of potential assistants based on provided instructions and required tools. It compares the input `instructions` against each candidate's instructions, and verifies that the set of required tool types and function names matches those available in the assistant's defined tools. Returns a list containing only the assistants that satisfy all matching criteria.*


### _process_assistant_config (method, L526-L543, parent: GPTAssistantAgent)

> *Summary: This method merges configuration settings from two input dictionaries, `llm_config` and `assistant_config`, into separate output configurations for the LLM client and the assistant. It prioritizes moving specific assistant-related keys found in the LLM config over the assistant config to maintain backward compatibility.*

