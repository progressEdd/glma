# autogen/agentchat/contrib/captainagent/tools/math/simplify_mixed_numbers.py

1 function(s): simplify_mixed_numbers.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| simplify_mixed_numbers | function |  |

## Chunks

### simplify_mixed_numbers (function, L4-L36)

> *Summary: Calculates the sum of two input mixed numbers by converting them to improper fractions, adding them using Python's `Fraction` type, and then formatting the final result as a string like "a b/c" or just an integer. It accepts six integers representing the numerators, denominators, and whole number parts of the two mixed numbers.*

