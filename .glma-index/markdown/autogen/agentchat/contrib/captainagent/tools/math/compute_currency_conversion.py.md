# autogen/agentchat/contrib/captainagent/tools/math/compute_currency_conversion.py

1 function(s): compute_currency_conversion.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| compute_currency_conversion | function |  |

## Chunks

### compute_currency_conversion (function, L8-L23)

> *Summary: Calculates a currency conversion by dividing an input `amount` (float) by a provided `exchange_rate` (float). It uses `sympy.Rational` for precise calculation before returning the result as a standard Python float.*

