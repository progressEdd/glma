# autogen/agentchat/group/reply_result.py

1 class(es): ReplyResult. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| ReplyResult | class |  |

## Chunks

### ReplyResult (class, L15-L24)

> *Summary: This data structure encapsulates the outcome of a tool invocation, holding the resulting message and optional transition or context variables. It is designed to communicate both the returned content and the next state in a conversational flow.*


### __str__ (method, L22-L24, parent: ReplyResult)

> *Summary: Returns a string representation of the object containing only its internal `message` attribute. This allows the result to be easily printed as plain text.*

