# test/beta/tools/test_naming.py

8 function(s): test_simple_tool, test_tool_schema_ignores_di, test_override_options, test_ensure_tools, test_ensure_tool_from_tool, test_option_description, test_empty_args, test_create_dynamic_options.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_simple_tool | function |  |
| test_tool_schema_ignores_di | function |  |
| test_override_options | function |  |
| test_ensure_tools | function |  |
| test_ensure_tool_from_tool | function |  |
| test_option_description | function |  |
| test_empty_args | function |  |
| test_create_dynamic_options | function |  |

## Chunks

### test_simple_tool (function, L42-L48)

> *Summary: This test verifies that a simple decorated tool conforms to the default schema structure. It defines a basic function with string and integer inputs, asserts its generated schema matches `DEFAULT_SCHEMA`, and expects no return value from the assertion itself.*


### test_tool_schema_ignores_di (function, L51-L64)

> *Summary: This test verifies that the generated tool schema correctly ignores dependency injection annotations (`Depends` and `Inject`). It defines a sample tool with these dependencies and asserts its resulting schema matches a predefined default structure.*


### test_override_options (function, L67-L79)

> *Summary: This test verifies that applying a `@tool` decorator correctly overrides the default name and description of a decorated function. It asserts that the resulting tool schema reflects these custom metadata changes for an input function accepting a string and an integer, returning a string.*


### test_ensure_tools (function, L82-L87)

> *Summary: This test verifies that the `FunctionTool.ensure_tool` method correctly generates a schema matching `DEFAULT_SCHEMA` when provided with an arbitrary function. It uses a simple mock function to validate the resulting tool definition structure.*


### test_ensure_tool_from_tool (function, L90-L96)

> *Summary: This test verifies that the `ensure_tool` function correctly wraps a provided tool definition, asserting that the resulting schema matches a predefined default structure. It takes a decorated function (`my_tool`) as input and checks the schema of the returned `FunctionTool`.*


### test_option_description (function, L99-L127)

> *Summary: This test verifies that a decorated function's schema is correctly generated by `FunctionTool.ensure_tool`. It asserts the resulting JSON schema accurately reflects the input parameters, including their descriptions and constraints like minimum values.*


### test_empty_args (function, L130-L145)

> *Summary: This test verifies that a tool defined with no arguments correctly generates a schema where the `parameters` object has a `"type"` of `"null"`. It asserts the resulting dictionary structure matches the expected OpenAPI specification for an argument-less function.*


### test_create_dynamic_options (function, L149-L175)

> *Summary: This test verifies that a decorated function is correctly converted into a structured tool schema. It asserts the resulting dictionary matches the expected OpenAPI-like structure for a function with optional string and arbitrary keyword arguments.*

