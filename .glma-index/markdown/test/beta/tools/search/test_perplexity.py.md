# test/beta/tools/search/test_perplexity.py

3 function(s): _tool_call_config, _search_response, _chat_response. 6 class(es): TestSchema, TestSearch, TestAnswer, TestSearchVariable, TestAnswerVariable, TestIndividualTools. 26 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| _tool_call_config | function |  |
| _search_response | function |  |
| _chat_response | function |  |
| TestSchema | class |  |
| TestSearch | class |  |
| TestAnswer | class |  |
| TestSearchVariable | class |  |
| TestAnswerVariable | class |  |
| TestIndividualTools | class |  |

## Chunks

### _tool_call_config (function, L28-L41)

> *Summary: Constructs a `TestConfig` object simulating a model response that includes a specific tool call based on provided arguments and tool name. It wraps the tool call details within a `ModelResponse` structure alongside an optional final reply string.*


### _search_response (function, L44-L45)

> *Summary: Wraps a list of search results into a standardized response dictionary. It ensures the output always contains an empty list for the `results` key if no input is provided.*


### _chat_response (function, L48-L73)

> *Summary: Constructs a standardized chat completion response dictionary, accepting optional content, search results, citations, and images as inputs. It packages these elements into a structured output mimicking an API response format.*


### TestSchema (class, L93-L140)

> *Summary: This test class verifies the structure and content of schemas generated by a `PerplexitySearchToolkit`. It asserts that default and custom-configured toolkits produce expected function names, descriptions, and required parameters for search and answer functionalities when provided with a context object.*


### test_default_schemas (method, L94-L100, parent: TestSchema)

> *Summary: This test verifies that a `PerplexitySearchToolkit` correctly returns the expected set of default function schemas when provided with a context object. It asserts that the returned list contains exactly two schema names: `"perplexity_search"` and `"perplexity_answer"`.*


### test_search_schema_has_query_param (method, L102-L111, parent: TestSchema)

> *Summary: This test verifies that the `PerplexitySearchToolkit` exposes a schema for its search function containing a required string parameter named "query". It achieves this by fetching all schemas from the toolkit and asserting the structure of the specific search function's parameters.*


### test_answer_schema_has_query_param (method, L113-L122, parent: TestSchema)

> *Summary: This test verifies that the `perplexity_answer` function schema within a search toolkit correctly requires and defines a string parameter named "query". It achieves this by retrieving all schemas from the toolkit using a provided context and asserting the structure of the target function's parameters.*


### test_custom_search_name_and_description (method, L124-L131, parent: TestSchema)

> *Summary: This test verifies that a custom name and description provided during a search request are correctly reflected in the returned function schema. It initializes a toolkit, calls `search` with specific metadata, and asserts the resulting schema matches those inputs.*


### test_custom_answer_name_and_description (method, L133-L140, parent: TestSchema)

> *Summary: This test verifies that a custom answer configuration correctly sets the function name and description when retrieved from the toolkit using provided context. It asserts that the resulting schema reflects the specified `"ask_perplexity"` name and `"Custom answer."` description.*


### TestSearch (class, L144-L267)

> *Summary: These tests verify the functionality of an AI agent interacting with a Perplexity search toolkit by mocking API responses. They confirm that the agent correctly handles structured results, empty results, and accurately forwards various parameters—including custom tool names and client-specific arguments—to the underlying search service.*


### test_returns_structured_results (method, L146-L175, parent: TestSearch)

> *Summary: This test verifies that an agent correctly processes structured search results from the Perplexity API when invoked. It mocks a successful API response and asserts that the resulting `ToolResultsEvent` contains the expected data structure with specific search results for the query "AG2 framework".*


### test_empty_results (method, L178-L189, parent: TestSearch)

> *Summary: This test verifies the agent's behavior when a search query yields no results from the Perplexity API. It mocks an empty response and asserts that the resulting tool output correctly reflects zero search results for the input "nothing".*


### test_none_params_omitted (method, L192-L202, parent: TestSearch)

> *Summary: This test verifies that when an agent is prompted to search, it correctly constructs and sends a POST request to the Perplexity API with the expected query parameter. It asserts that the request body sent by the agent matches the predefined input structure.*


### test_all_params_forwarded (method, L205-L235, parent: TestSearch)

> *Summary: This test verifies that an agent correctly forwards all specified parameters when invoking the Perplexity search tool. It mocks a successful API response and asserts that the actual HTTP request sent to the search endpoint contains every input parameter provided during tool instantiation.*


### test_client_kwargs_forwarded_to_sdk (method, L238-L250, parent: TestSearch)

> *Summary: This test verifies that custom keyword arguments provided to the client are correctly forwarded to the underlying SDK when an agent invokes a search tool. It mocks an external API call and asserts that the configured request was made using the specified custom URL.*


### test_custom_tool_name_in_agent (method, L253-L267, parent: TestSearch)

> *Summary: This test verifies that an agent correctly invokes a custom search tool when prompted. It sets up a mock HTTP response for the Perplexity API and asserts that the configured `web_search` tool is called by the agent during execution.*


### TestAnswer (class, L271-L509)

> *Summary: These tests verify the functionality of an agent interacting with a Perplexity search toolkit by mocking API responses. They confirm that the agent correctly processes structured results, handles empty outputs, forwards various parameters to the underlying API call, and accurately extracts both text and image data from the tool's output.*


### test_returns_structured_results (method, L273-L311, parent: TestAnswer)

> *Summary: This test verifies that the agent correctly processes and structures results from a mocked Perplexity API call. It asserts that the returned `ToolResultsEvent` contains a `PerplexitySearchResponse` object populated with expected search results, content, and citations based on the mock response.*


### test_empty_results (method, L314-L327, parent: TestAnswer)

> *Summary: This test verifies the agent's behavior when a search query yields no results from the Perplexity API. It mocks a successful HTTP response containing empty search data and asserts that the resulting tool output reflects this empty state for the given input query.*


### test_defaults_applied_when_params_omitted (method, L330-L348, parent: TestAnswer)

> *Summary: This test verifies that default parameters are correctly applied when none are explicitly provided during an API call. It mocks a POST request to the Perplexity completions endpoint and asserts that the resulting request body contains predefined values like `"sonar"` model, specific system/user messages, and context size settings.*


### test_all_params_forwarded (method, L351-L388, parent: TestAnswer)

> *Summary: This test verifies that an agent correctly forwards all specified parameters when calling the Perplexity search tool. It mocks a successful API response and asserts that the outgoing request body contains all configured options like model name, token limits, and specific search filters.*


### test_client_kwargs_forwarded_to_sdk (method, L391-L405, parent: TestAnswer)

> *Summary: This test verifies that custom API key and base URL arguments provided to a `PerplexitySearchToolkit` are correctly forwarded when an `Agent` uses the tool to make an external HTTP request. It asserts that the mocked endpoint was successfully called during the agent's execution.*


### test_custom_tool_name_in_agent (method, L408-L422, parent: TestAnswer)

> *Summary: This test verifies that an agent correctly invokes a custom-named search tool when prompted. It mocks the external API call and asserts that the configured tool was called by the agent during execution.*


### test_returns_image_parts_when_api_yields_images (method, L425-L485, parent: TestAnswer)

> *Summary: When the underlying API returns a response containing images, this test verifies that the agent correctly processes and outputs both textual content and structured image data parts. It simulates an API call with two mock images and asserts that the resulting tool output contains the expected text alongside separate `ImageInput` objects for each provided URL.*


### test_skips_image_entries_without_image_url (method, L488-L509, parent: TestAnswer)

> *Summary: This test verifies that an agent correctly ignores entries lacking an `image_url` when processing search results from the Perplexity API. It mocks a successful API response containing one valid and one invalid (no URL) image entry, asserting that only the valid image is passed to the final output.*


### TestSearchVariable (class, L513-L553)

> *Summary: These tests verify the behavior of an AI agent when interacting with a Perplexity search tool. The first test confirms that the agent correctly substitutes dynamic variables (like `user_max` and `search_recency_filter`) into the API request payload, while the second test ensures the agent raises a `KeyError` if required input variables are missing.*


### test_resolved (method, L515-L538, parent: TestSearchVariable)

> *Summary: This test verifies that an agent correctly constructs and sends a search request to the Perplexity API. It mocks the external API call and asserts that the outgoing HTTP body contains the expected query, maximum results (7), and recency filter ("day") derived from the agent's configuration.*


### test_missing_raises (method, L541-L553, parent: TestSearchVariable)

> *Summary: This test verifies that an `Agent` correctly raises a `KeyError` when the required `max_results` parameter is missing during a search operation. It simulates a successful API response while intentionally omitting the necessary input for the tool call to trigger the expected error.*


### TestAnswerVariable (class, L557-L604)

> *Summary: This test suite verifies the behavior of an AI agent interacting with a Perplexity search tool. It asserts that when provided necessary variables, the agent correctly constructs and sends the expected API request to the external service; conversely, it confirms that missing required configuration variables raises a `KeyError`.*


### test_resolved (method, L559-L587, parent: TestAnswerVariable)

> *Summary: This test verifies the correct API call structure when an agent uses a Perplexity search tool. It mocks a successful HTTP response and asserts that the generated request body sent to the Perplexity endpoint contains the expected model, query, and configuration parameters derived from the agent's setup.*


### test_missing_raises (method, L590-L604, parent: TestAnswerVariable)

> *Summary: This test verifies that the system raises a `KeyError` when attempting to use an answer tool without specifying a search mode. It mocks a successful API response and then asserts that calling the agent with a prompt fails due to the missing configuration for `search_mode`.*


### TestIndividualTools (class, L608-L657)

> *Summary: These tests verify the functionality of a `PerplexitySearchToolkit` by simulating API calls using `respx`. They confirm that an agent correctly invokes specific search or answer tools, either when passed individually or as part of the full toolkit.*


### test_search_tool_passed_alone (method, L610-L624, parent: TestIndividualTools)

> *Summary: This test verifies that an agent correctly invokes the search tool when prompted with a specific query. It mocks the external API call and asserts that the request sent to the search endpoint contains the expected query parameter.*


### test_answer_tool_passed_alone (method, L627-L640, parent: TestIndividualTools)

> *Summary: This test verifies that an agent correctly invokes the Perplexity answer tool when prompted with a specific query. It mocks the external API call to ensure the agent successfully interacts with the configured toolkit and makes the expected HTTP request.*


### test_whole_toolkit_registers_both_tools (method, L643-L657, parent: TestIndividualTools)

> *Summary: This test verifies that a toolkit correctly registers both necessary tools when integrated into an agent. It mocks the external search API and asserts that the agent sends the expected query payload to the mocked endpoint upon invocation.*

