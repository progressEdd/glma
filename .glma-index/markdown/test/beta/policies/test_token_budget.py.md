# test/beta/policies/test_token_budget.py

2 function(s): _tool_response, _tool_results. 3 class(es): TestNoTrimming, TestTrimming, TestOrphanedToolResults. 7 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| _tool_response | function |  |
| _tool_results | function |  |
| TestNoTrimming | class |  |
| TestTrimming | class |  |
| TestOrphanedToolResults | class |  |

## Chunks

### _tool_response (function, L20-L24)

> *Summary: Generates a mock response object simulating a tool execution result. It constructs a `ModelResponse` containing a single `ToolCallEvent` with the provided call ID and tool name.*


### _tool_results (function, L27-L30)

> *Summary: Creates a `ToolResultsEvent` containing a single `ToolResultEvent`. This event signals the completion of a tool execution with a success status ("ok") for the specified parent and tool name.*


### TestNoTrimming (class, L33-L42)

> *Summary: This test verifies that when input events are within the token budget, the policy applies no changes to them. It passes a list of model requests and asserts that the resulting events match the original inputs and that no prompts were generated.*


### test_events_within_budget_are_unchanged (method, L35-L42, parent: TestNoTrimming)

> *Summary: When applying a token budget policy to a list of input events with a large allowance, the function asserts that the original events remain unchanged and no prompts are generated. This verifies that events within the defined budget pass through without modification or triggering prompt generation.*


### TestTrimming (class, L45-L68)

> *Summary: This test suite verifies the behavior of a token budgeting policy when processing sequences of model requests. It asserts that the policy correctly retains only the most recent events under a tight budget and transparently prepends existing context to subsequent events when configured.*


### test_retains_most_recent_events (method, L47-L56, parent: TestTrimming)

> *Summary: This test verifies that a token budget policy correctly retains only the most recent event when the total input exceeds the allowed limit. It simulates an input stream with two requests and asserts that the final output contains only the content from the last request.*


### test_transparent_adds_prompt (method, L59-L68, parent: TestTrimming)

> *Summary: This test verifies that a transparent token budget policy correctly modifies input requests when the budget is exceeded. It applies the policy to a list of model requests, asserting that one request remains unchanged while the other is augmented with a specific budget notification string.*


### TestOrphanedToolResults (class, L71-L155)

> *Summary: This test suite verifies that a token budget policy correctly drops leading and mid-window `ToolResultsEvents` if they are orphaned (i.e., lack a corresponding tool use). It uses various input event sequences and tight budgets to assert the resulting filtered list maintains data integrity while respecting token limits.*


### test_leading_orphaned_tool_result_is_skipped (method, L75-L95, parent: TestOrphanedToolResults)

> *Summary: This test verifies that an orphaned `ToolResultsEvent` appearing before sufficient budget is available will be skipped by the token budget policy. It simulates a sequence of requests and tool results with a tight budget to ensure only subsequent, affordable events are processed.*


### test_multiple_leading_orphaned_tool_results_are_skipped (method, L98-L116, parent: TestOrphanedToolResults)

> *Summary: This test verifies that the `TokenBudgetPolicy` correctly skips multiple leading "orphaned" tool results when processing a sequence of model requests and tool responses. It asserts that two specific, unreferenced tool result events are dropped from the final output stream.*


### test_transparent_count_reflects_skipped_orphans (method, L119-L134, parent: TestOrphanedToolResults)

> *Summary: This test verifies that a `TokenBudgetPolicy` with transparent mode correctly accounts for skipped orphaned items when processing a sequence of model requests and tool results. It asserts that the final prompt generated by the policy reflects the removal of an orphan item from the input stream.*


### test_mid_window_orphaned_tool_result_is_dropped (method, L137-L155, parent: TestOrphanedToolResults)

> *Summary: This test verifies that an orphaned `ToolResultsEvent` is discarded by the token budget policy even when it's not at the start of the processing window. It simulates a sequence of requests and tool results to confirm only valid model requests are retained in the final output.*

