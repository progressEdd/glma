# test/beta/a2a/conftest.py

2 function(s): server_url, local_skills_dir.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| server_url | function |  |
| local_skills_dir | function |  |

## Chunks

### server_url (function, L11-L12)

> *Summary: Provides a hardcoded string representing the base URL for testing purposes. This function returns a static `str` value, `"http://test"`, to be used across tests.*


### local_skills_dir (function, L16-L29)

> *Summary: This function sets up a temporary directory structure by creating two distinct skill directories, each containing an `SKILL.md` file with predefined metadata. It returns the root of this populated temporary path for use in tests.*

