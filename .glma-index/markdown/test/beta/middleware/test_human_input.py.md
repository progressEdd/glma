# test/beta/middleware/test_human_input.py

1 function(s): test_config. 4 class(es): MockHumanInputMiddleware, OrderingHumanInputMiddleware, TestHumanInputMiddleware, TestHumanInputHook. 13 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_config | function |  |
| MockHumanInputMiddleware | class |  |
| OrderingHumanInputMiddleware | class |  |
| TestHumanInputMiddleware | class |  |
| TestHumanInputHook | class |  |

## Chunks

### test_config (function, L17-L21)

> *Summary: Creates and returns a `TestConfig` object, initializing it with a specific tool call event named "my\_tool" and the string "result". This function serves to set up configuration for testing purposes.*


### MockHumanInputMiddleware (class, L24-L43)

> *Summary: This middleware intercepts human input requests by using a provided mock object to simulate entering the request content before calling the next handler. It then uses the same mock object to exit with the response content generated by the downstream handler, returning the final message.*


### __init__ (method, L25-L32, parent: MockHumanInputMiddleware)

> *Summary: Initializes the object by accepting a base event, context, and a mock object. It stores the provided mock for later use within the instance.*


### on_human_input (method, L34-L43, parent: MockHumanInputMiddleware)

> *Summary: This method intercepts a human input request by mocking the content before calling the next handler in the chain. It then uses the resulting message's content to exit the mock state and returns the final processed message.*


### OrderingHumanInputMiddleware (class, L46-L67)

> *Summary: This middleware wraps a human input hook to manage mock state around the execution of the next handler in the chain. It takes an event and context, uses a provided mock object to enter and exit a specific position before and after calling the next function.*


### __init__ (method, L47-L56, parent: OrderingHumanInputMiddleware)

> *Summary: Initializes an object by storing a base event, context, a mock object, and a specific integer position. This setup prepares the instance for testing middleware behavior using provided dependencies.*


### on_human_input (method, L58-L67, parent: OrderingHumanInputMiddleware)

> *Summary: This method acts as a middleware hook that wraps the next handler execution. It enters and exits a mock state around calling `call_next` with the provided request and context, ultimately returning the resulting message.*


### TestHumanInputMiddleware (class, L70-L210)

> *Summary: These tests verify the behavior of an agent interacting with human input through various middleware configurations. They demonstrate how different middlewares can intercept, mutate requests and responses, enforce call ordering, or short-circuit the standard interaction flow when `agent.ask()` is called.*


### test_basic (method, L72-L91, parent: TestHumanInputMiddleware)

> *Summary: This test verifies the basic interaction flow when human input is required during an agent's execution. It sets up an agent with a tool that prompts for input and uses a mock middleware to capture the request and simulate the response from a HITL hook.*


### test_call_sequence (method, L94-L113, parent: TestHumanInputMiddleware)

> *Summary: This test verifies the execution order of middleware when an agent interacts with a human-in-the-loop (HITL) system. It asserts that specific middleware layers are entered and exited in a defined sequence during the `agent.ask()` call.*


### test_mutates_request (method, L116-L145, parent: TestHumanInputMiddleware)

> *Summary: This test verifies that a custom middleware modifies the input content before it reaches the HITL hook. It sets up an agent with three instances of a mutating middleware, which appends "!" to any incoming human input, and asserts that the mock HITL function receives the modified string.*


### test_mutates_response (method, L148-L176, parent: TestHumanInputMiddleware)

> *Summary: This test verifies that a custom middleware modifies the final response content by appending an exclamation mark. It sets up an agent with multiple instances of this mutating middleware and asserts that the mocked input is called with the modified string, "answer!!!".*


### test_short_circuits (method, L179-L210, parent: TestHumanInputMiddleware)

> *Summary: This test verifies that a custom middleware correctly short-circuits the agent's execution when human input is requested. It asserts that the mock interception method was called with the expected input, while confirming that the HITL hook was never invoked.*


### TestHumanInputHook (class, L213-L288)

> *Summary: These tests verify how an `Agent` interacts with human input hooks during execution. They confirm that the provided hook (either via agent configuration or directly in the `.ask()` call) correctly dictates the value returned when a tool requests user input, demonstrating override precedence.*


### test_returns_raw_string (method, L215-L232, parent: TestHumanInputHook)

> *Summary: This test verifies that an agent using a human-in-the-loop hook returns the raw string provided by that hook when prompted. It sets up an agent with a mock tool and a specific `hitl_hook` to assert the correct value is used during execution.*


### test_returns_raw_string_async (method, L235-L252, parent: TestHumanInputHook)

> *Summary: This test verifies that an agent correctly utilizes a human-in-the-loop hook when prompted. It sets up an agent with a mock tool and a custom hook, then asserts the hook's return value was used as the final response to the agent's query.*


### test_passed_at_ask (method, L255-L268, parent: TestHumanInputHook)

> *Summary: This test verifies that when an agent is prompted, it correctly triggers a human-in-the-loop (HITL) hook by asserting the mock was called with the expected "ask-level answer" string. It achieves this by injecting a mocked input function into a custom tool used by the agent during the `agent.ask()` call.*


### test_at_ask_overrides_agent (method, L271-L288, parent: TestHumanInputHook)

> *Summary: This test verifies that a specific human input hook overrides the agent's internal HITL hook when an `agent.ask()` method is called. It sets up an agent with tools and hooks, then asserts that the outermost provided hook is invoked instead of the one defined within the agent configuration.*

