# test/beta/middleware/test_agent_turns.py

4 class(es): MockMiddleware, TestAgentTurnMiddleware, TestInsertMiddleware, TestAddMiddleware. 9 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| MockMiddleware | class |  |
| TestAgentTurnMiddleware | class |  |
| TestInsertMiddleware | class |  |
| TestAddMiddleware | class |  |

## Chunks

### MockMiddleware (class, L15-L38)

> *Summary: This class wraps an agent turn execution to simulate external interactions using a provided mock object. It intercepts the `on_turn` method, calling mock entry before proceeding with the next handler and then calling mock exit afterward.*


### __init__ (method, L16-L27, parent: MockMiddleware)

> *Summary: Initializes an agent turn test by setting up the event and context, then configures a provided mock object using the input event. It stores references to the mock and the current iteration position for subsequent use.*


### on_turn (method, L29-L38, parent: MockMiddleware)

> *Summary: This method wraps the execution of the next agent turn by entering and exiting a mock state at the current position before passing the event and context to `call_next`. It returns whatever response is generated by the subsequent middleware in the chain.*


### TestAgentTurnMiddleware (class, L42-L91)

> *Summary: These tests verify the behavior of an `Agent` when wrapped with various middleware configurations. They confirm that a single input triggers correct model request creation, validates sequential message passing through multiple middlewares, and demonstrates how custom middlewares can mutate incoming requests and outgoing responses.*


### test_creation (method, L44-L53, parent: TestAgentTurnMiddleware)

> *Summary: This test verifies that an `Agent` correctly invokes the underlying model creation mechanism when asked a question. It asserts that the mock's `create` method is called exactly once with a request containing the input text "Hi!".*


### test_chaining (method, L55-L65, parent: TestAgentTurnMiddleware)

> *Summary: This test verifies the execution order of middleware when an agent processes a prompt. It asserts that the `enter` hook is called sequentially by each configured middleware (1 through 3) and the `exit` hook is called in reverse order (3 through 1).*


### test_incoming_message_mutation (method, L67-L91, parent: TestAgentTurnMiddleware)

> *Summary: This test verifies that a custom middleware mutates incoming `ModelRequest` messages by doubling the content and subsequently doubles the final response content. It asserts that the tracking mechanism records the heavily mutated initial request and that the agent returns the fully doubled result.*


### TestInsertMiddleware (class, L95-L115)

> *Summary: These tests verify the correct execution order of middleware when inserted into an agent's processing pipeline. They assert that `enter` and `exit` methods on mocked middleware are called with specific sequence numbers based on insertion position relative to existing middleware.*


### test_is_called (method, L96-L102, parent: TestInsertMiddleware)

> *Summary: This test verifies that a mocked middleware's `enter` method is called exactly once when an agent processes the input "Hi!". It achieves this by injecting the mock into the agent's middleware chain and awaiting an interaction.*


### test_inserted_is_outer (method, L104-L115, parent: TestInsertMiddleware)

> *Summary: This test verifies the execution order of middleware when a new layer is inserted into an agent's pipeline. It asserts that the middleware at position 1 executes before the original middleware at position 2 during both entry and exit phases.*


### TestAddMiddleware (class, L119-L139)

> *Summary: These tests verify the correct invocation order of middleware when adding and querying an agent. Specifically, one test confirms a single middleware is called upon execution, while another asserts that added middlewares execute in the specified positional order during both entry and exit phases.*


### test_is_called (method, L120-L126, parent: TestAddMiddleware)

> *Summary: This test verifies that a mocked middleware's `enter` method is called exactly once when an agent processes the input "Hi!". It sets up an agent with mock middleware and asserts the call count after invoking the agent's `ask` method.*


### test_added_is_inner (method, L128-L139, parent: TestAddMiddleware)

> *Summary: This test verifies middleware execution order by adding two middlewares to an agent and then calling `ask`. It asserts that the entry points are called sequentially based on their defined positions (1 then 2), while exit points are called in reverse order (2 then 1).*

