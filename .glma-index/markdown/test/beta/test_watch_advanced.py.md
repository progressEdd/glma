# test/beta/test_watch_advanced.py

1 function(s): test_cron_watch_arm_and_disarm. 1 class(es): TestCadenceWatchTimeTrigger. 4 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestCadenceWatchTimeTrigger | class |  |
| test_cron_watch_arm_and_disarm | function |  |

## Chunks

### TestCadenceWatchTimeTrigger (class, L16-L97)

> *Summary: This test suite verifies the behavior of a `CadenceWatch` mechanism used to collect specific events from an agent's stream output. It asserts that the watcher correctly collects matching events within a time window, ignores non-matching events, and stops collecting when explicitly disarmed.*


### test_collects_events_in_window (method, L17-L51, parent: TestCadenceWatchTimeTrigger)

> *Summary: This test verifies that a `CadenceWatch` correctly collects events within a specified time window when an agent processes requests. It asserts that the collected list of events matches the expected sequence of tool calls generated by the agent interaction.*


### test_ignores_non_matching (method, L53-L67, parent: TestCadenceWatchTimeTrigger)

> *Summary: This test verifies that a `CadenceWatch` configured to only trigger on `ToolCallEvent` ignores incoming events from an agent's interaction with a memory stream. It asserts that no batches are recorded when the agent's output does not match the specified condition.*


### test_disarm_cancels_timer (method, L69-L84, parent: TestCadenceWatchTimeTrigger)

> *Summary: This test verifies that calling `disarm()` on a watch immediately cancels any pending timer events. It asserts that no event batches are collected after the disarming action, even when an agent is subsequently asked for data.*


### test_disarm_resets_armed_flag (method, L86-L97, parent: TestCadenceWatchTimeTrigger)

> *Summary: This test verifies that calling the `disarm` method on a `CadenceWatch` instance correctly sets its internal armed state to false. It initializes the watcher, asserts it is armed, calls `disarm`, and then confirms the armed flag has been reset.*


### test_cron_watch_arm_and_disarm (function, L101-L112)

> *Summary: This test verifies the arming and disarming functionality of a `CronWatch` instance configured for a specific schedule. It asserts that the watch state correctly transitions from armed to disarmed after calling the `disarm()` method.*

