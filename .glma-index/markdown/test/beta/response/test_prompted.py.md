# test/beta/response/test_prompted.py

4 class(es): TestPromptedSchemaFromType, TestPromptedSchemaFromResponseProto, TestSystemPrompt, TestValidation. 17 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestPromptedSchemaFromType | class |  |
| TestPromptedSchemaFromResponseProto | class |  |
| TestSystemPrompt | class |  |
| TestValidation | class |  |

## Chunks

### TestPromptedSchemaFromType (class, L16-L46)

> *Summary: This test suite verifies that `PromptedSchema` correctly generates a descriptive prompt based on the input type. It confirms that schemas derived from primitive types, dataclasses, and Pydantic models accurately reflect the expected structure within the generated system prompt.*


### test_from_primitive_type (method, L17-L23, parent: TestPromptedSchemaFromType)

> *Summary: This test verifies that a `PromptedSchema` initialized with a primitive type correctly sets its name and system prompt, while ensuring the JSON schema remains unset. It confirms the system prompt contains the expected type definition for integers.*


### test_from_dataclass (method, L25-L35, parent: TestPromptedSchemaFromType)

> *Summary: This test verifies that a `PromptedSchema` correctly introspects a dataclass, ensuring the resulting schema object accurately reflects the class name and includes all field names within its system prompt string.*


### test_from_pydantic_model (method, L37-L46, parent: TestPromptedSchemaFromType)

> *Summary: This test verifies that a `PromptedSchema` correctly extracts and embeds the field names from an input Pydantic model into its system prompt. It asserts that the generated schema name matches the model's name and that all defined fields are present within the system prompt string.*


### TestPromptedSchemaFromResponseProto (class, L49-L71)

> *Summary: This test suite verifies that `PromptedSchema` correctly wraps a `ResponseSchema`, preserving its name, description, and underlying JSON schema structure within the resulting object's system prompt or attributes. It confirms that wrapping maintains data integrity across different input configurations.*


### test_wraps_response_schema (method, L50-L56, parent: TestPromptedSchemaFromResponseProto)

> *Summary: This test verifies that a `PromptedSchema` correctly wraps an existing `ResponseSchema`. It asserts that the wrapper retains the original name and description, while also confirming the system prompt contains the expected type information for integers.*


### test_wraps_schema_with_description (method, L58-L62, parent: TestPromptedSchemaFromResponseProto)

> *Summary: This test verifies that a `PromptedSchema` correctly inherits and exposes the `description` from its contained `ResponseSchema`. It asserts that the outer schema's description matches the inner schema's provided string.*


### test_inner_schema_preserved (method, L64-L71, parent: TestPromptedSchemaFromResponseProto)

> *Summary: This test verifies that when a `ResponseSchema` is wrapped in a `PromptedSchema`, the internal JSON schema remains unchanged, while the public `json_schema` attribute is set to `None`. It confirms the wrapper preserves the underlying structure of the input schema.*


### TestSystemPrompt (class, L74-L116)

> *Summary: This test suite verifies the behavior of a system prompt generator by testing various configurations of `PromptedSchema`. It asserts that prompts correctly embed JSON schemas, adhere to default formatting rules, respect custom templates, and return `None` when no inner schema is present.*


### test_contains_json_schema (method, L75-L87, parent: TestSystemPrompt)

> *Summary: This test verifies that a generated system prompt includes the complete JSON schema derived from a provided configuration dataclass. It achieves this by comparing the serialized `ResponseSchema`'s JSON structure against the content of the `PromptedSchema`'s system prompt.*


### test_uses_default_template (method, L89-L94, parent: TestSystemPrompt)

> *Summary: Verifies that the system prompt generated by a `PromptedSchema` using an integer type includes specific instructions regarding JSON format and extraneous text exclusion. It asserts the presence of these required phrases within the retrieved system prompt string.*


### test_custom_prompt_template (method, L96-L101, parent: TestSystemPrompt)

> *Summary: This test verifies that a custom prompt template correctly constructs the system prompt by prepending a specific instruction to the provided schema definition. It asserts that the resulting `system_prompt` begins with the expected "Return JSON: ```" prefix.*


### test_no_prompt_when_no_inner_schema (method, L103-L116, parent: TestSystemPrompt)

> *Summary: Verifies that when a `PromptedSchema` is initialized with an inner schema lacking a JSON schema, the resulting system prompt remains unset (`None`). This test confirms the expected behavior for schemas without structural constraints.*


### TestValidation (class, L120-L188)

> *Summary: This test suite verifies the `PromptedSchema`'s ability to validate input strings against various target types, including primitives, dataclasses, Pydantic models, and nested schemas/callables. It confirms successful parsing for valid inputs while asserting exceptions are raised when invalid JSON or type mismatches occur.*


### test_validates_primitive (method, L121-L126, parent: TestValidation)

> *Summary: This test verifies that a `PromptedSchema` configured for integers correctly parses and validates JSON input containing an integer value. It asserts that the returned result matches the expected primitive type from the input string.*


### test_validates_dataclass (method, L128-L138, parent: TestValidation)

> *Summary: This test verifies that a `PromptedSchema` correctly validates JSON input against a defined dataclass structure. It takes a JSON string as input and asserts the output matches an instantiated instance of the target dataclass.*


### test_validates_pydantic_model (method, L140-L151, parent: TestValidation)

> *Summary: This test verifies that a `PromptedSchema` correctly parses and validates JSON input against a provided Pydantic model. It asserts that the returned object is an instance of the expected model and contains the correct data from the input string.*


### test_delegates_to_inner_response_schema (method, L153-L159, parent: TestValidation)

> *Summary: This test verifies that a `PromptedSchema` correctly delegates validation to its inner `ResponseSchema`. It asserts that validating the input string `'{"data": 42}'` against the schema yields the integer value `42`.*


### test_delegates_to_inner_callable_schema (method, L161-L170, parent: TestValidation)

> *Summary: This test verifies that a `PromptedSchema` correctly delegates validation to an inner callable decorated with `@response_schema`. It passes the string "21" as input and asserts the resulting validated output is 42.*


### test_validation_error_on_invalid_json (method, L172-L177, parent: TestValidation)

> *Summary: This test asserts that an exception is raised when attempting to validate the string `"not a number"` against a schema expecting an integer type. It uses `AsyncMock` for context and verifies the expected failure during validation.*


### test_callable_schema_validation_error (method, L179-L188, parent: TestValidation)

> *Summary: This test verifies that the `PromptedSchema` correctly raises an exception when provided with invalid input for a callable schema. It attempts to validate the string `"not a number"` against a function expecting an integer conversion, asserting that an error is thrown.*

