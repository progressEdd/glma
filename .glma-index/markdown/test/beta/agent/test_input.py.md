# test/beta/agent/test_input.py

2 function(s): test_agent_ask_accepts_text_input, test_turn_ask_accepts_text_input.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_agent_ask_accepts_text_input | function |  |
| test_turn_ask_accepts_text_input | function |  |

## Chunks

### test_agent_ask_accepts_text_input (function, L13-L18)

> *Summary: This test verifies that the agent correctly processes text input by calling `agent.ask()` with a `TextInput` object. It asserts that the returned response body matches the expected configuration result.*


### test_turn_ask_accepts_text_input (function, L22-L28)

> *Summary: This test verifies that an agent correctly processes text input during a conversational turn. It initializes an agent, sends an initial message, and then accepts subsequent text input to assert the final state of the response body.*

