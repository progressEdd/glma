# test/beta/config/openai/tools/test_mcp_server.py

1 function(s): test_completions_api_unsupported. 1 class(es): TestResponsesApi. 6 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_completions_api_unsupported | function |  |
| TestResponsesApi | class |  |

## Chunks

### test_completions_api_unsupported (function, L14-L20)

> *Summary: This test verifies that the API correctly raises an `UnsupportedToolError` when provided with a schema generated by the `MCPServerTool`. It achieves this by first fetching schemas from the tool using the provided context and then passing those schemas to the API function.*


### TestResponsesApi (class, L23-L121)

> *Summary: This test suite verifies the serialization of `MCPServerTool` configurations into a standardized API response format. It asserts that various inputs, such as allowed tools, custom headers, authorization tokens, and descriptions, are correctly mapped to the output dictionary structure, including precedence rules for authentication.*


### test_defaults (method, L25-L35, parent: TestResponsesApi)

> *Summary: This test verifies the default configuration of an `MCPServerTool` by fetching its schemas using a provided context. It asserts that the resulting schema structure correctly reflects the initialized server URL and label, along with a fixed approval requirement.*


### test_with_allowed_tools (method, L38-L53, parent: TestResponsesApi)

> *Summary: This test verifies that an `MCPServerTool` correctly exposes its configuration when queried for schemas using a provided context. It asserts the resulting schema structure matches the expected format, including specified server details and allowed tools.*


### test_with_headers (method, L56-L71, parent: TestResponsesApi)

> *Summary: This test verifies that the `MCPServerTool` correctly serializes its configuration, including custom HTTP headers. It calls the tool's schema retrieval method and asserts the resulting structure matches the initialized parameters.*


### test_auth_token_becomes_header (method, L74-L89, parent: TestResponsesApi)

> *Summary: This test verifies that an authorization token provided to the `MCPServerTool` is correctly transformed into an HTTP header when generating API responses. It asserts that the resulting schema includes the correct `Authorization: Bearer <token>` header based on the input configuration.*


### test_headers_take_precedence_over_auth_token (method, L92-L103, parent: TestResponsesApi)

> *Summary: This test verifies that custom HTTP headers override the provided authorization token when fetching schemas from an MCP server. It asserts that the resulting API response correctly reflects the specified `Custom auth` header, ignoring the initial `token123`.*


### test_with_description (method, L106-L121, parent: TestResponsesApi)

> *Summary: This test verifies that an `MCPServerTool` correctly generates a specific API response structure when queried with a given context. It asserts the resulting schema matches expected values like the provided URL, label, and description.*

