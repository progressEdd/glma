# test/beta/config/openai/tools/test_unsupported.py

2 class(es): TestCompletionsApi, TestResponsesApi. 11 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestCompletionsApi | class |  |
| TestResponsesApi | class |  |

## Chunks

### TestCompletionsApi (class, L20-L91)

> *Summary: These asynchronous tests verify that various specialized tools (like `WebSearchTool`, `CodeExecutionTool`, etc.) correctly raise an `UnsupportedToolError` when their generated schemas are passed to the API conversion function. The input is a testing context, and the expected output is an exception indicating tool incompatibility.*


### test_web_search (method, L22-L28, parent: TestCompletionsApi)

> *Summary: This test verifies that an unsupported tool raises the expected error when passed to the API interface. It initializes a `WebSearchTool`, retrieves its schema using provided context, and then asserts that calling `tool_to_api` with this schema fails with an `UnsupportedToolError`.*


### test_web_fetch (method, L31-L37, parent: TestCompletionsApi)

> *Summary: This test verifies that an unsupported tool raises the expected error when passed to `tool_to_api`. It initializes a `WebFetchTool`, retrieves its schema using the provided context, and then asserts that calling `tool_to_api` with this schema fails with an `UnsupportedToolError`.*


### test_code_execution (method, L40-L46, parent: TestCompletionsApi)

> *Summary: This test verifies that attempting to use a `CodeExecutionTool` schema with the API raises an `UnsupportedToolError`. It achieves this by first retrieving the tool's schemas from a given context and then passing one of those schemas to the API function.*


### test_shell (method, L49-L55, parent: TestCompletionsApi)

> *Summary: This test verifies that an unsupported tool schema raises an `UnsupportedToolError` when passed to the API interface. It initializes a `ShellTool`, retrieves its schemas from a given context, and then asserts the expected exception during the API call.*


### test_memory (method, L58-L64, parent: TestCompletionsApi)

> *Summary: This test verifies that an unsupported tool raises an `UnsupportedToolError` when passed to the API layer. It initializes a `MemoryTool`, retrieves its schemas using provided context, and then asserts the expected exception during the API call simulation.*


### test_image_generation (method, L67-L73, parent: TestCompletionsApi)

> *Summary: This test verifies that an unsupported tool raises the expected `UnsupportedToolError` when passed to the API layer. It achieves this by first retrieving the schema for an `ImageGenerationTool` using a provided context and then attempting to process that schema.*


### test_mcp_server (method, L76-L82, parent: TestCompletionsApi)

> *Summary: This test verifies that an `MCPServerTool` fails when its schema is passed to the API conversion function. It asserts that calling `tool_to_api` with the tool's generated schemas raises an `UnsupportedToolError`.*


### test_skills (method, L85-L91, parent: TestCompletionsApi)

> *Summary: This test verifies that an attempt to pass a schema generated by `SkillsTool` for the "pptx" type to `tool_to_api` raises an `UnsupportedToolError`. It confirms the tool correctly signals when its output is not supported by the API conversion logic.*


### TestResponsesApi (class, L94-L120)

> *Summary: These tests verify that specific tools, including `WebFetchTool`, `MemoryTool`, and `SkillsTool`, correctly raise an `UnsupportedToolError` when their generated schemas are passed to the `tool_to_responses_api` function. The asynchronous methods confirm this failure behavior for various tool types using a provided testing context.*


### test_web_fetch (method, L96-L102, parent: TestResponsesApi)

> *Summary: This test verifies that an unsupported tool schema raises an `UnsupportedToolError` when passed to the `tool_to_responses_api`. It initializes a `WebFetchTool`, retrieves its schemas using a provided context, and then asserts the expected exception during API processing.*


### test_memory (method, L105-L111, parent: TestResponsesApi)

> *Summary: This test verifies that an unsupported tool raises an `UnsupportedToolError` when its schema is passed to the `tool_to_responses_api`. It initializes a `MemoryTool`, retrieves its schemas using a provided context, and then asserts the expected exception during API conversion.*


### test_skills (method, L114-L120, parent: TestResponsesApi)

> *Summary: This test verifies that an unsupported tool, specifically one for "pptx", correctly raises an `UnsupportedToolError` when passed to the API response handler after retrieving its schema from a given context. It asserts the expected exception is raised during the processing of the retrieved schema.*

