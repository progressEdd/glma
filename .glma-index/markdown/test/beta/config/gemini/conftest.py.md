# test/beta/config/gemini/conftest.py

1 function(s): gemini_config.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| gemini_config | function |  |

## Chunks

### gemini_config (function, L11-L14)

> *Summary: Provides a mock configuration object for Gemini testing, setting the `api_key` attribute to `"test-key"` upon invocation. This allows tests to run without requiring actual API credentials.*

