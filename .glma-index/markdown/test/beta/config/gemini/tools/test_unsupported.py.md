# test/beta/config/gemini/tools/test_unsupported.py

5 function(s): test_shell, test_memory, test_image_generation, test_mcp_server, test_skills.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_shell | function |  |
| test_memory | function |  |
| test_image_generation | function |  |
| test_mcp_server | function |  |
| test_skills | function |  |

## Chunks

### test_shell (function, L18-L24)

> *Summary: This test verifies that attempting to build tools using a schema generated by `ShellTool` raises an `UnsupportedToolError`. It initializes the tool, retrieves its schemas from the provided context, and then asserts the expected exception when passing those schemas to the tool building function.*


### test_memory (function, L28-L34)

> *Summary: This test verifies that attempting to build tools using a schema generated by `MemoryTool` raises an `UnsupportedToolError`. It takes a `Context` object as input and asserts the expected exception when calling `build_tools` with the retrieved schema.*


### test_image_generation (function, L38-L44)

> *Summary: This test verifies that attempting to build tools using an `ImageGenerationTool` schema raises an `UnsupportedToolError`. It achieves this by first retrieving the tool's schemas from a given context and then passing those schemas to the tool building function within an expected exception block.*


### test_mcp_server (function, L48-L54)

> *Summary: This test verifies that attempting to build tools using a schema generated by an `MCPServerTool` results in an `UnsupportedToolError`. It initializes the tool with a specific server URL and then asserts the expected failure when passing its output schema to the tool building function.*


### test_skills (function, L58-L64)

> *Summary: This test verifies that attempting to build tools using a schema derived from the `SkillsTool` initialized with "pptx" raises an `UnsupportedToolError`. It asserts this failure by calling `tool.schemas()` and then passing the resulting schema to `build_tools()`.*

