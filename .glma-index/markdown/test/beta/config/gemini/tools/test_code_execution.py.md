# test/beta/config/gemini/tools/test_code_execution.py

1 function(s): test_defaults.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_defaults | function |  |

## Chunks

### test_defaults (function, L14-L21)

> *Summary: This test verifies that when requesting schemas from the `CodeExecutionTool` using a given context, the resulting schema correctly constructs a tool definition containing code execution capabilities. It asserts that the output matches an expected structure wrapping the code execution type within a list of tools.*

