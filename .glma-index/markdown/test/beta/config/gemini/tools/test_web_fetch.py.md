# test/beta/config/gemini/tools/test_web_fetch.py

1 function(s): test_defaults.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_defaults | function |  |

## Chunks

### test_defaults (function, L14-L21)

> *Summary: This test verifies that the `WebFetchTool` correctly generates a list containing a single `Tool` instance with a default `UrlContext` when provided with a testing context. It asserts that the resulting structure matches the expected output from building tools based on the tool's schemas.*

