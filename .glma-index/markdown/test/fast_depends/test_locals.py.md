# test/fast_depends/test_locals.py

2 function(s): wrap, test_locals.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| wrap | function |  |
| test_locals | function |  |

## Chunks

### wrap (function, L13-L14)

> *Summary: This function acts as a simple wrapper, taking an input function and returning the result of passing it through another `inject` utility. It essentially delegates the wrapping logic to the `inject` mechanism.*


### test_locals (function, L17-L25)

> *Summary: This test verifies the behavior of a wrapper function applied to a method that takes and returns an instance of a Pydantic `BaseModel`. It executes the wrapped method with a specific model instance as input.*

