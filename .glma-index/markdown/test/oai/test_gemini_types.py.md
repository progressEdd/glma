# test/oai/test_gemini_types.py

2 function(s): _remove_descriptions, _check_schema_subset. 1 class(es): TestGeminiTypes. 7 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| _remove_descriptions | function |  |
| _check_schema_subset | function |  |
| TestGeminiTypes | class |  |

## Chunks

### _remove_descriptions (function, L10-L25)

> *Summary: Recursively traverses a schema dictionary to strip out any keys named "description". It processes nested dictionaries and lists containing dictionaries, returning a cleaned version of the input structure.*


### _check_schema_subset (function, L28-L76)

> *Summary: This function verifies if a `google_schema` is a subset of a `local_schema`, recursively checking nested dictionaries and lists for compatibility. It accepts two schema dictionaries and returns a list of strings detailing any missing fields or value mismatches found during the comparison.*


### TestGeminiTypes (class, L100-L178)

> *Summary: This test suite verifies the structural compatibility between locally defined data models and their corresponding Google SDK counterparts across various types like `LatLng`, configuration objects, and enums. It asserts schema equivalence for several model pairs and confirms specific behaviors for case-insensitive enum comparisons.*


### test_FunctionCallingConfigMode (method, L101-L103, parent: TestGeminiTypes)

> *Summary: This test verifies that the enumeration values for function calling configuration modes are identical between `LocalFunctionCallingConfigMode` and `FunctionCallingConfigMode`. It iterates through a predefined set of mode strings to assert this equivalence.*


### test_LatLng (method, L105-L111, parent: TestGeminiTypes)

> *Summary: Verifies that the local `LocalLatLng` schema is a superset of the Google SDK's `LatLng` schema by comparing their JSON schemas and asserting no incompatibilities exist. This ensures forward compatibility between the two data structures.*


### test_FunctionCallingConfig (method, L113-L119, parent: TestGeminiTypes)

> *Summary: Verifies that the local function calling configuration schema is a compatible subset of the Google SDK's expected schema. It achieves this by comparing the JSON schemas after stripping descriptions and asserting no incompatibilities exist.*


### test_RetrievalConfig (method, L121-L127, parent: TestGeminiTypes)

> *Summary: Verifies that the local retrieval configuration schema is a compatible subset of the Google SDK's retrieval configuration schema. It achieves this by comparing the JSON schemas after stripping out descriptions and asserting no incompatibilities exist.*


### test_ToolConfig (method, L129-L135, parent: TestGeminiTypes)

> *Summary: Verifies that the local tool configuration schema is a compatible subset of the Google SDK's `ToolConfig` schema. It achieves this by comparing the JSON schemas after stripping out descriptions and asserting no incompatibilities exist.*


### test_CaseInSensitiveEnum (method, L137-L166, parent: TestGeminiTypes)

> *Summary: This test verifies that enums inheriting from `CaseInSensitiveEnum` perform case-insensitive comparisons when initialized with string values. It asserts equality between instances created using different casing conventions across local and base enum types, while ensuring the underlying `.value` remains consistent.*


### test_CommonBaseModel (method, L168-L178, parent: TestGeminiTypes)

> *Summary: Verifies that the JSON schemas generated by a local and a common base model are structurally identical after removing their respective "title" fields. This ensures consistent schema representation between the two implementations, despite potential naming differences in metadata.*

