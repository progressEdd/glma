# test/events/test_print_event.py

1 class(es): TestPrintEvent. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestPrintEvent | class |  |

## Chunks

### TestPrintEvent (class, L11-L27)

> *Summary: This test verifies the functionality of a `PrintEvent` by asserting its structure matches an expected dictionary representation and confirming that calling its `print` method invokes the underlying print function with the correct arguments, including setting `flush=True`. It uses mocks to isolate and validate this behavior.*


### test_print (method, L12-L27, parent: TestPrintEvent)

> *Summary: This test verifies the functionality of a `PrintEvent` by instantiating it with specific parameters and asserting its internal structure matches an expected dictionary representation. It further confirms that calling the event's `print` method correctly invokes the underlying print function with the appropriate arguments, including setting `flush=True`.*

