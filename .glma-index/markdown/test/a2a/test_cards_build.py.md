# test/a2a/test_cards_build.py

7 function(s): test_default_card, test_custom_autocreated_card, test_card_settings_overrides_agent, test_card_settings_overrides_url, test_extended_card, test_extended_card_settings_overrides_agent, test_extended_card_settings_overrides_url.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_default_card | function |  |
| test_custom_autocreated_card | function |  |
| test_card_settings_overrides_agent | function |  |
| test_card_settings_overrides_url | function |  |
| test_extended_card | function |  |
| test_extended_card_settings_overrides_agent | function |  |
| test_extended_card_settings_overrides_url | function |  |

## Chunks

### test_default_card (function, L11-L20)

> *Summary: This test verifies the default card generated by an A2aAgentServer, ensuring its name matches the provided agent's name and its URL is set to a local host address. It also confirms that the card's description reflects the agent's description and that it does not support authenticated extended cards by default.*


### test_custom_autocreated_card (function, L23-L31)

> *Summary: This test verifies that an `A2aAgentServer` correctly constructs a card object from its inputs. It asserts that the generated card accurately reflects the agent's name, description, and the server's URL.*


### test_card_settings_overrides_agent (function, L34-L47)

> *Summary: This test verifies that an agent's associated card settings are correctly overridden when initializing the A2aAgentServer with specific `CardSettings`. It asserts that the server's exposed card object reflects these provided overrides.*


### test_card_settings_overrides_url (function, L50-L62)

> *Summary: This test verifies that the `A2aAgentServer` correctly uses a specified URL for its associated card settings, even when an agent is provided as input. It asserts that the server's internal card URL matches the one configured during initialization.*


### test_extended_card (function, L65-L79)

> *Summary: This test verifies that an `A2aAgentServer` correctly generates and exposes an extended agent card based on provided settings. It asserts that the resulting card accurately reflects the agent's name, description, and server URL.*


### test_extended_card_settings_overrides_agent (function, L82-L95)

> *Summary: This test verifies that a specified `CardSettings` object correctly overrides the default agent card configuration when initializing an A2aAgentServer with a `ConversableAgent`. It asserts that the server's exposed card matches the provided custom settings.*


### test_extended_card_settings_overrides_url (function, L98-L110)

> *Summary: This test verifies that an `A2aAgentServer` correctly uses a specified URL for its extended agent card settings when initialized with custom configuration. It asserts the server's internal card URL matches the provided input value, while expecting a `RuntimeWarning` during setup.*

