# test/agents/contrib/time/test_timetoolagent.py

1 class(es): TestTimeToolAgent. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestTimeToolAgent | class |  |

## Chunks

### TestTimeToolAgent (class, L12-L60)

> *Summary: Verifies that a `TimeToolAgent` initializes correctly, ensuring its internal format string, available tools (specifically `date_time`), and system message match expected configurations based on provided credentials. It also confirms that overriding the system message during instantiation successfully changes the agent's behavior instruction.*


### test_init (method, L13-L60, parent: TestTimeToolAgent)

> *Summary: Verifies that a `TimeToolAgent` initializes correctly with specific configurations, asserting its internal format string, available tools (specifically `date_time`), and system message based on provided inputs. It also confirms that overriding the system message during instantiation takes precedence.*

