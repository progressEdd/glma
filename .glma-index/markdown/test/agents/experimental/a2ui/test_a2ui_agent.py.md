# test/agents/experimental/a2ui/test_a2ui_agent.py

4 class(es): TestA2UIAgent, TestA2UIAgentValidationRetry, TestValidateA2UIResponse, TestBuildRetryMessages. 39 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestA2UIAgent | class |  |
| TestA2UIAgentValidationRetry | class |  |
| TestValidateA2UIResponse | class |  |
| TestBuildRetryMessages | class |  |

## Chunks

### TestA2UIAgent (class, L12-L265)

> *Summary: This test suite verifies the functionality of an `A2UIAgent` by instantiating it with various configurations and asserting correct behavior. It tests initialization, system message generation (including custom messages and schema inclusion), protocol version handling, action definition for both event and function call types, and response parsing capabilities.*


### test_default_init (method, L13-L17, parent: TestA2UIAgent)

> *Summary: This test verifies the default initialization of an `A2UIAgent` instance, asserting that the provided name is correctly set and that the internal protocol version and catalog ID match expected values.*


### test_system_message_contains_a2ui (method, L19-L25, parent: TestA2UIAgent)

> *Summary: This test verifies that the system message generated by an `A2UIAgent` instance contains specific required strings, including "A2UI", "v0.9", and "createSurface". It confirms the agent is correctly configured with its expected operational context.*


### test_custom_system_message_prepended (method, L27-L35, parent: TestA2UIAgent)

> *Summary: This test verifies that the `A2UIAgent` initializes with a custom system message prepended to its internal message structure. It asserts that both the provided custom text and a standard format string are present at the beginning of the agent's system message.*


### test_unsupported_version_raises (method, L37-L39, parent: TestA2UIAgent)

> *Summary: Asserts that instantiating an agent with a specific unsupported protocol version raises a `ValueError` containing the expected message. This test verifies the agent's validation logic when provided with an outdated or unrecognized version string.*


### test_custom_catalog_id_from_catalog (method, L41-L48, parent: TestA2UIAgent)

> *Summary: This test verifies that an agent correctly adopts a custom catalog ID provided during initialization. It asserts that the `catalog_id` attribute matches the specified URL and that this URL is reflected within the agent's system message.*


### test_custom_catalog_without_id_raises (method, L50-L56, parent: TestA2UIAgent)

> *Summary: Asserts that instantiating an agent with a custom catalog lacking component IDs raises a `ValueError`. This test verifies the input validation logic for the agent's configuration.*


### test_custom_delimiter (method, L58-L65, parent: TestA2UIAgent)

> *Summary: Verifies that an `A2UIAgent` initialized with a custom response delimiter correctly incorporates it into its system message while excluding the default JSON marker. This test confirms proper configuration handling for specialized output formatting.*


### test_exclude_schema_from_prompt (method, L67-L73, parent: TestA2UIAgent)

> *Summary: When initialized with `include_schema_in_prompt=False`, the resulting agent's system message should explicitly exclude any mention of the "A2UI Message Schema." This test verifies that schema information is omitted from the prompt context under this configuration.*


### test_exclude_rules_from_prompt (method, L75-L81, parent: TestA2UIAgent)

> *Summary: When initialized with `include_rules_in_prompt=False`, the resulting agent's system message should explicitly exclude any mention of "Component Rules." This test verifies that rule inclusion is correctly suppressed during agent setup.*


### test_schema_manager_accessible (method, L83-L86, parent: TestA2UIAgent)

> *Summary: Verifies that an initialized `A2UIAgent` instance possesses a non-null `schema_manager`, and confirms this manager adheres to the expected protocol version ("v0.9").*


### test_response_parser_accessible (method, L88-L92, parent: TestA2UIAgent)

> *Summary: Verifies that the agent's response parser is initialized and correctly processes a string lacking A2UI content, asserting that the parsing result indicates no A2UI was found.*


### test_response_parser_extracts_a2ui (method, L94-L103, parent: TestA2UIAgent)

> *Summary: This test verifies that the response parser correctly identifies and extracts A2UI data from a string input containing a specific marker. It asserts that the parsed result indicates the presence of A2UI content and contains exactly one operation.*


### test_update_system_message_preserves_a2ui (method, L105-L111, parent: TestA2UIAgent)

> *Summary: This test verifies that updating the system message on an `A2UIAgent` correctly incorporates the new instruction while retaining essential formatting directives like "A2UI Response Format" and "createSurface". It asserts that the agent's updated system message begins with the provided custom prompt.*


### test_update_system_message_no_duplicate (method, L113-L119, parent: TestA2UIAgent)

> *Summary: This test verifies that when a custom system message is updated, the pre-defined A2UI prompt section is included exactly once within the final system message. It achieves this by manually injecting the agent's internal A2UI section into the update call and asserting the count of a specific phrase.*


### test_a2ui_prompt_section_property (method, L121-L125, parent: TestA2UIAgent)

> *Summary: This test verifies that the `a2ui_prompt_section` property of an initialized `A2UIAgent` contains expected strings, specifically `"A2UI Response Format"` and `"v0.9"`. It confirms the agent's prompt structure adheres to predefined standards upon instantiation.*


### test_validation_retries_property (method, L127-L129, parent: TestA2UIAgent)

> *Summary: This test verifies that an instantiated `A2UIAgent` correctly exposes the configured number of response validation retries via its `validation_retries` property when initialized with a specific value. It asserts that the internal state matches the input parameter set during agent creation.*


### test_import_from_experimental (method, L131-L134, parent: TestA2UIAgent)

> *Summary: This test verifies that importing `A2UIAgent` from the experimental module correctly resolves to the original class object. It asserts identity between the imported and expected agent classes.*


### test_action_type_defaults_to_event (method, L136-L138, parent: TestA2UIAgent)

> *Summary: Verifies that an `A2UIAction` instance defaults its `action_type` to `"event"` when no specific type is provided during initialization. The test takes a basic action object and asserts the default value of its type attribute.*


### test_event_action_in_prompt (method, L140-L157, parent: TestA2UIAgent)

> *Summary: Verifies that the system message generated by an `A2UIAgent` correctly includes references to "Server Events," the defined action name ("book\_table"), and the concept of an event, while excluding mentions of client functions. This test confirms the agent's prompt structure aligns with expected behavioral patterns for handling events.*


### test_function_call_action_in_prompt (method, L159-L176, parent: TestA2UIAgent)

> *Summary: This test verifies that the system message generated by an `A2UIAgent` correctly includes instructions for client functions, specifically checking for mentions of `"Client Functions"` and `"openUrl"`. It asserts that server event instructions are absent from the resulting prompt.*


### test_function_call_prompt_uses_call_not_name (method, L178-L195, parent: TestA2UIAgent)

> *Summary: Verifies that the system message generated by an `A2UIAgent` correctly uses `"call"` instead of `"name"` when defining a function call action, adhering to the A2UI v0.9 specification. It asserts the presence of both `"call":` and `"returnType"` within the agent's system prompt.*


### test_function_call_validates_against_schema (method, L197-L227, parent: TestA2UIAgent)

> *Summary: This test verifies that an A2UI response containing a function call action passes schema validation when the `validate_responses` flag is set to true. It inputs a specific JSON-formatted string representing UI components and asserts that parsing succeeds without any reported errors.*


### test_mixed_actions_in_prompt (method, L229-L251, parent: TestA2UIAgent)

> *Summary: This test verifies that an agent initialized with a mix of custom and function-calling actions correctly incorporates descriptions for both types into its system message. It asserts the presence of specific keywords related to server events, client functions, and the defined action names within the generated prompt.*


### test_get_action_both_types (method, L253-L265, parent: TestA2UIAgent)

> *Summary: This test verifies that an agent correctly retrieves actions based on their names, distinguishing between different action types like "event" and "functionCall." It confirms the retrieval mechanism works for existing actions while returning `None` for non-existent ones.*


### TestA2UIAgentValidationRetry (class, L268-L413)

> *Summary: This test suite validates the retry mechanism within an agent responsible for validating A2UI responses. It uses mocked LLM calls to simulate scenarios where responses are immediately valid, plain text, invalid but recoverable via retries, or permanently invalid after exhausting all configured attempts.*


### _make_agent (method, L271-L277, parent: TestA2UIAgentValidationRetry)

> *Summary: Constructs and returns a configured `A2UIAgent` instance for testing purposes. It initializes the agent with specific settings like using "gpt-4o-mini" and enables response validation, allowing control over retry attempts.*


### test_valid_response_passes_through (method, L279-L300, parent: TestA2UIAgentValidationRetry)

> *Summary: This test verifies that when a valid A2UI response is provided, the agent immediately returns it without retrying. It mocks the reply generation to return a specific string and asserts that the agent processes this input correctly in one call.*


### test_plain_text_bypasses_validation (method, L302-L312, parent: TestA2UIAgentValidationRetry)

> *Summary: This test verifies that responses containing only plain text bypass the A2UI validation process. It mocks the generation to return simple text and asserts that the agent processes this input without flagging any issues.*


### test_retry_on_invalid_then_success (method, L314-L341, parent: TestA2UIAgentValidationRetry)

> *Summary: This test verifies that the agent retries when an initial response is invalid. It mocks the generation process to return an invalid JSON string on the first call and a valid one on the second, asserting that the final output reflects the successful retry.*


### test_retry_exhausted_returns_text_only (method, L343-L358, parent: TestA2UIAgentValidationRetry)

> *Summary: When the underlying generation process fails after exhausting all configured retries, this test verifies that the agent returns only the plain text content from the response. It achieves this by mocking a generator to return an invalid JSON structure and asserting the final reply is just the preceding text.*


### test_retry_feeds_back_errors (method, L360-L391, parent: TestA2UIAgentValidationRetry)

> *Summary: This test verifies that when an agent receives a validation error from the first attempt, it correctly retries by feeding back the error to the user. It mocks the generation process to simulate one invalid response followed by one valid response and asserts the resulting message history includes the original prompt, the assistant's initial failure, and the subsequent user feedback containing "validation errors."*


### test_json_parse_error_triggers_retry (method, L393-L413, parent: TestA2UIAgentValidationRetry)

> *Summary: This test verifies that an invalid JSON response from the underlying service correctly triggers a retry mechanism. It mocks the generation function to return broken JSON on the first call and valid JSON on the second, asserting that the agent successfully processes the final valid reply after two attempts.*


### TestValidateA2UIResponse (class, L416-L458)

> *Summary: This test suite verifies the response validation logic of an agent by testing its `_validate_a2ui_response` helper. It checks that plain text yields no A2UI structure, valid A2UI responses pass without errors, invalid A2UI structures generate specific errors, and malformed JSON results in a parsing error.*


### _make_agent (method, L419-L424, parent: TestValidateA2UIResponse)

> *Summary: Instantiates and returns a configured `A2UIAgent` instance with specific test settings. This method creates the agent object using predefined parameters like name and validation status.*


### test_plain_text_returns_no_errors (method, L426-L430, parent: TestValidateA2UIResponse)

> *Summary: When provided with simple string input, the agent's validation method should return no errors and indicate that the response does not conform to A2UI structure. This test verifies correct handling of non-structured text data.*


### test_valid_a2ui_returns_no_errors (method, L432-L441, parent: TestValidateA2UIResponse)

> *Summary: This test verifies that a valid A2UI response string processed by the agent yields no validation errors and correctly indicates the presence of A2UI data. It takes a predefined, well-formed JSON response as input and asserts the resulting parsed object confirms its validity.*


### test_invalid_a2ui_returns_errors (method, L443-L451, parent: TestValidateA2UIResponse)

> *Summary: When provided with a malformed A2UI response string lacking necessary fields like `catalogId`, the function validates it and asserts that non-empty error messages are returned. This confirms the agent correctly identifies invalid input structures.*


### test_invalid_json_returns_parse_error (method, L453-L458, parent: TestValidateA2UIResponse)

> *Summary: When provided with a response string containing malformed JSON within the designated section, this test verifies that the validation process correctly identifies and returns a parsing error. It asserts that an error object exists and specifically contains "Invalid JSON".*


### TestBuildRetryMessages (class, L461-L496)

> *Summary: This test suite verifies the `_build_retry_messages` helper function by ensuring it correctly constructs a sequence of messages from an initial user prompt, a response text, parsing results, and any validation errors. It confirms that the output message list is correctly structured and that the original input messages remain unmodified after processing.*


### _make_agent (method, L464-L469, parent: TestBuildRetryMessages)

> *Summary: Instantiates and returns a configured `A2UIAgent` instance with specific test settings. This method creates the agent object using predefined parameters like name and validation status.*


### test_appends_response_and_feedback (method, L471-L486, parent: TestBuildRetryMessages)

> *Summary: This test verifies that an agent correctly constructs a sequence of messages when processing user input, a parsed response, and associated validation errors. It asserts the resulting message list contains the original prompt, the assistant's response, and a final user message detailing the encountered errors.*


### test_does_not_mutate_original (method, L488-L496, parent: TestBuildRetryMessages)

> *Summary: Verifies that the `_build_retry_messages` method does not modify the input message list. It passes an initial user message and a parsed result to confirm the original list remains unchanged after processing.*

