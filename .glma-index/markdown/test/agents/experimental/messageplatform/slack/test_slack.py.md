# test/agents/experimental/messageplatform/slack/test_slack.py

1 class(es): TestSlackAgent. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestSlackAgent | class |  |

## Chunks

### TestSlackAgent (class, L15-L81)

> *Summary: This test verifies the initialization of a Slack agent, ensuring it correctly configures its available tools and system prompt. It asserts that the agent possesses `slack_send` and `slack_retrieve` functions with predefined schemas, along with a specific instructional system message tailored for Slack communication.*


### test_init (method, L16-L81, parent: TestSlackAgent)

> *Summary: This test verifies the initialization of a Slack agent by asserting that it correctly configures its available tools and system prompt. It confirms the agent possesses `slack_send` and `slack_retrieve` functions, receives valid LLM configuration, and is given specific formatting guidelines for Slack communication.*

