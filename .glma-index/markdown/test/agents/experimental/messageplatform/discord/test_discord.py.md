# test/agents/experimental/messageplatform/discord/test_discord.py

1 class(es): TestDiscordAgent. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestDiscordAgent | class |  |

## Chunks

### TestDiscordAgent (class, L12-L68)

> *Summary: This test verifies the initialization of a Discord agent by asserting its configuration. It checks that the agent possesses specific `discord_send` and `discord_retrieve` tools, has the correct LLM configuration structure, and is initialized with a predefined system message guiding its behavior within Discord.*


### test_init (method, L13-L68, parent: TestDiscordAgent)

> *Summary: This test verifies the initialization of a Discord agent by asserting that it correctly includes specific tools (`discord_send` and `discord_retrieve`) in its configuration. It also confirms the presence of a predefined system message guiding the agent's behavior within Discord.*

