# test/interop/pydantic_ai/test_pydantic_ai_tool.py

1 class(es): TestPydanticAITool. 1 method(s).

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| TestPydanticAITool | class |  |

## Chunks

### TestPydanticAITool (class, L14-L62)

> *Summary: This test verifies that a function wrapped by `PydanticAITool` is correctly registered with an `AssistantAgent`. It takes a Python function as input and asserts that the agent's configuration now includes the corresponding OpenAI tool definition derived from the function's signature.*


### test_register_for_llm (method, L15-L62, parent: TestPydanticAITool)

> *Summary: This test verifies that a function wrapped by `PydanticAITool` is correctly registered with an `AssistantAgent`. It takes a Python function as input and asserts that the agent's configuration now includes the corresponding OpenAI tool definition derived from the function's schema.*

