# test/agentchat/contrib/test_captainagent.py

2 function(s): test_captain_agent_from_scratch, test_captain_agent_with_library.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| test_captain_agent_from_scratch | function |  |
| test_captain_agent_with_library | function |  |

## Chunks

### test_captain_agent_from_scratch (function, L21-L56)

> *Summary: This test function initializes a `CaptainAgent` with specific LLM and nested configurations, then uses a `UserProxyAgent` to start a multi-turn chat session. It executes the agent against a complex task requiring programming and domain analysis, returning the final chat result object.*


### test_captain_agent_with_library (function, L61-L101)

> *Summary: This test sets up and executes a chat session using a `CaptainAgent` configured with specific LLM and build settings. It initiates the conversation via a `UserProxyAgent` with a complex task prompt to verify the agent's functionality.*

