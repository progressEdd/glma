# test/agentchat/extensions/tsp_api.py

2 function(s): change_dist, compare_costs.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| change_dist | function |  |
| compare_costs | function |  |

## Chunks

### change_dist (function, L12-L27)

> *Summary: Modifies a given distance matrix by updating the cost between two specified nodes. It takes the matrix, source index ($i$), destination index ($j$), and the new cost as input, returning the original cost that was replaced.*


### compare_costs (function, L30-L40)

> *Summary: Calculates the relative change between two floating-point costs. It takes a previous and a new cost as input and returns the fractional difference, representing how much the cost has changed from the prior value.*

