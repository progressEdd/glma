# examples/live_playground/live_gemini.py

2 function(s): sum_numbers, main.

## Key Exports

| Name | Type | Description |
| ---- | ---- | ----------- |
| sum_numbers | function |  |
| main | function |  |

## Chunks

### sum_numbers (function, L14-L20)

> *Summary: This asynchronous function takes two integers as input and returns a `ToolResult` containing the string representation of their sum. It prints a message indicating which numbers are being added before returning the result.*


### main (function, L35-L45)

> *Summary: This asynchronous function initializes and runs an agent, concurrently managing audio input via a recorder and output via a player within the agent's context. It then streams and prints all incoming events from the agent until the stream is closed.*

